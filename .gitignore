# --- Local-only working files (never commit) ---
docs/PLAN.md
*.local.md
settings.local.json

# --- Secrets & config ---
.env
.env.*
appsettings.*.local.json
*.pfx
*.key

# --- Local data & logs ---
fsops.db
fsops.db-*
*.db-shm
*.db-wal
logs/
*.log

# --- .NET build output ---
bin/
obj/
publish/
*.user
.vs/

# Publish and installer output (scripts/publish.ps1, installer/). Both are large - a publish is
# roughly 190 MB and an installer 60-100 MB - and both are regenerated from source on demand.
artifacts/

# --- MSFS SDK package-tool output ---
# The compiled panel/InGamePanels/FSOpsPanel.spb IS committed - it is the whole point of the
# build. These are the tool's scratch and staging folders, which are regenerated every run.
src/fsops-ingame-panel/source/_out/
src/fsops-ingame-panel/source/_Temp/

# --- Node / frontend ---
node_modules/
dist/
.vite/
*.tsbuildinfo

# Frontend build output (generated by `npm run build`, produced at publish time)
src/FSOps.Server/wwwroot/*
!src/FSOps.Server/wwwroot/.gitkeep

# --- OS / editor noise ---
Thumbs.db
Desktop.ini
.DS_Store
*.swp
