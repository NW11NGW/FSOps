# Architecture

This document describes how FSOps is put together: the solution layout, how a request or a piece of sim data flows through the system, and the design principles the codebase is built around.

## Table of contents

- [Solution layout](#solution-layout)
- [FSOps.Core areas](#fsopscore-areas)
- [API endpoint surface](#api-endpoint-surface)
- [Request and data flow](#request-and-data-flow)
- [Design principles](#design-principles)
  - [Local-first, structured to move online later](#local-first-structured-to-move-online-later)
  - [Money is stored in a single base unit](#money-is-stored-in-a-single-base-unit)
  - [Locale-pinned date formatting](#locale-pinned-date-formatting)
  - [Append-only ledger](#append-only-ledger)
  - [App paths: no hardcoded filesystem paths](#app-paths-no-hardcoded-filesystem-paths)
  - [Deterministic, testable planning logic](#deterministic-testable-planning-logic)
  - [Sim abstraction](#sim-abstraction)
- [Simulator connection and the telemetry pipeline](#simulator-connection-and-the-telemetry-pipeline)
- [Flight-phase detection and landing quality](#flight-phase-detection-and-landing-quality)
- [The append-only flight event log and crash recovery](#the-append-only-flight-event-log-and-crash-recovery)
- [Playstyles and EconomyConfigCatalog](#playstyles-and-economyconfigcatalog)
- [The monthly billing cycle: EconomyClockService](#the-monthly-billing-cycle-economyclockservice)
- [Fuel billing](#fuel-billing)
- [Maintenance scheduling](#maintenance-scheduling)
- [Virtual pilots and wall-clock resolution: VirtualFlightResolverService](#virtual-pilots-and-wall-clock-resolution-virtualflightresolverservice)
- [Reputation and pilot skill](#reputation-and-pilot-skill)
- [The weekly schedule builder: aircraft-per-duty-day](#the-weekly-schedule-builder-aircraft-per-duty-day)
- [Aircraft reservation: the two-way gate, and ReservationReconciler](#aircraft-reservation-the-two-way-gate-and-reservationreconciler)
- [Getting rid of an aircraft, and settling a loan: the quote-then-confirm pattern](#getting-rid-of-an-aircraft-and-settling-a-loan-the-quote-then-confirm-pattern)
- [Repositioning a stranded aircraft](#repositioning-a-stranded-aircraft)
- [The Finances page and the away-summary](#the-finances-page-and-the-away-summary)
- [The live operations map](#the-live-operations-map)
- [The Statistics page](#the-statistics-page)
- [SimBrief flight plan import](#simbrief-flight-plan-import)
- [The VATSIM ATC layer](#the-vatsim-atc-layer)
- [The desktop shell and how FSOps is published](#the-desktop-shell-and-how-fsops-is-published)
- [The update checker](#the-update-checker)
- [Testing](#testing)
- [Layering diagram](#layering-diagram)

## Solution layout

FSOps is a single .NET solution (`FSOps.sln`) with a React frontend alongside it:

| Project | Responsibility |
|---|---|
| `src/FSOps.Core` | Domain model, money handling, route planning, and finance calculations. Airlines, routes, aircraft, flights, pilots, and the entities and pure logic that drive them — see [FSOps.Core areas](#fsopscore-areas). No dependency on ASP.NET Core, EF Core, or SimConnect — this project is plain C# so it can be unit tested in isolation. |
| `src/FSOps.Data` | Persistence. Entity Framework Core mapping of the domain model onto SQLite, entity configurations, world data import, and the `FsOpsDbContext` used by the server. |
| `src/FSOps.Sim` | The sim abstraction and its two implementations: `SimConnectSource`, which wraps `CTrue.FsConnect` to read live aircraft state from a running copy of MSFS, and `FakeSimSource`, which replays a recorded flight from a JSON script with no simulator needed. Both implement the same `ISimSource` interface (see [Sim abstraction](#sim-abstraction)). |
| `src/FSOps.Server` | The ASP.NET Core host. Exposes the REST API (see [API endpoint surface](#api-endpoint-surface)) and SignalR hubs, runs the background services that pump sim telemetry and drive flight tracking (see [Simulator connection and the telemetry pipeline](#simulator-connection-and-the-telemetry-pipeline)), post the monthly billing cycle (see [The monthly billing cycle](#the-monthly-billing-cycle-economyclockservice)), and resolve virtual pilots' scheduled flights on the wall clock (see [Virtual pilots and wall-clock resolution](#virtual-pilots-and-wall-clock-resolution-virtualflightresolverservice)); also runs `ReservationReconciler` once on every startup, before those hosted services start, to repair any aircraft left both reserved-for-player and scheduled by a database written before that rule existed (see [Aircraft reservation](#aircraft-reservation-the-two-way-gate-and-reservationreconciler)); wires everything together via dependency injection, and serves the built frontend as static files. |
| `src/fsops-web` | The React + TypeScript frontend, built with Vite and styled with Tailwind CSS and shadcn/ui, with MapLibre GL for the route-network and live-flight maps. Runs entirely in the browser against the local server. |
| `tests/FSOps.Core.Tests` | xUnit tests, focused on `FSOps.Core`'s domain, planning, and flight-tracking logic (phase state machine, landing quality, aircraft-type matching, flight numbering). |
| `tests/FSOps.Server.Tests` | xUnit tests for server-level behaviour that needs a database, chiefly round-trip route pairing and airline-summary route counting. |

## FSOps.Core areas

`FSOps.Core` is organised into a handful of focused areas rather than one flat namespace:

| Area | Contents |
|---|---|
| `Entities/` | The domain model: `Airline`, `Route`, `FleetAircraft`, `Flight`, `FlightEvent`, `Pilot`, `PilotSchedule`/`PilotScheduleEntry`, `Loan`, `Lease`, `LedgerTransaction`, `MaintenanceEvent`, `EconomyState`, `Airport`, `Runway`, `AircraftType`, `UserSettings`, and the shared enums (strategy profile, units, currencies, statuses). `Flight.Revenue`/`TotalCost` are a read-time cache of what's actually been posted to `LedgerTransaction` for that flight - never the source of truth themselves (see [Append-only ledger](#append-only-ledger)) - and `Flight.RevenuePosted` is the idempotency gate that stops a retry or crash rehydration posting a flight's ledger lines twice. `FleetAircraft.ReservedForPlayer` is the sole, hard gate between the player and the virtual-pilot scheduler - see [Aircraft reservation](#aircraft-reservation-the-two-way-gate-and-reservationreconciler). `PilotSchedule` is the aggregate root for one virtual pilot's standing weekly schedule (created lazily, on first save), and carries `AutoSuspendOnMaintenance` (default `true`) - whether an occurrence whose aircraft is grounded for a check is recorded as the `FlightStatus.Suspended` value (distinct from `Skipped`/`Cancelled`, and never fee-bearing) rather than skipped or cancelled outright; `PilotScheduleEntry` is one leg in it - a day of the week, a departure time (UTC), a directional route leg, and the fleet aircraft that flies it. There's no end date: the week itself is the repeating unit. `EconomyState.AwaySummaryLastViewedUtc` is a cursor distinct from the billing/schedule watermarks (see below) - it tracks only how much of the ledger/flight/maintenance history has been shown to the player in a "while you were away" summary, and only `POST /away-summary/acknowledge` ever advances it. |
| `Economy/` | The economy engine: `EconomyConfig` (every tuning constant for one resolved playstyle) and `EconomyConfigCatalog` (loads `economy-config.json` once and resolves the two playstyle-specific configs from it - see [Playstyles and EconomyConfigCatalog](#playstyles-and-economyconfigcatalog)), `ReferenceFareCalculator`, `DemandCalculator` (city-pair passenger pool from catchment/distance/season/day/reputation), `FareDemandModel` (the anti-exploit fare/load-factor/revenue curve - see [Fare setting and demand response](guides/user-guide.md#the-economy-simulation) in the user guide), `FuelPricing` (per-airport, per-day fuel price), `TankeringAdvisor` (whether uplifting extra fuel here to skip a pricier refuel later would pay off - see [Persistent fuel and tankering](#persistent-fuel-and-tankering)), `MaintenanceScheduler` (A/C-check triggering, downtime and used-aircraft starting wear - see [Maintenance scheduling](#maintenance-scheduling)), `ReputationCalculator` (advances `Airline.ReputationScore` by one flight's outcome - see [Reputation and pilot skill](#reputation-and-pilot-skill)), `FlightCostCalculator` (weight- and size-based landing/handling/parking/passenger/turnaround fees, maintenance accrual, crew cost), and `FlightEconomicsCalculator`, which ties all of the above into one itemised `FlightEconomicsResult` for a single flight. Pure - no database, no I/O. `FSOps.Server.Services.FlightEconomicsPoster` is the only place that result becomes real `LedgerTransaction` rows: fuel is posted at flight start, only for what's actually uplifted (see [Persistent fuel and tankering](#persistent-fuel-and-tankering)); every other line posts on completion, gated on `Flight.RevenuePosted` for idempotency, and skipped entirely for a slew/position-jump-flagged sector rather than computed and discarded. Recurring monthly billing (lease, salary, insurance, loan amortisation) is a separate background service, `FSOps.Server.Services.EconomyClockService` - see [The monthly billing cycle](#the-monthly-billing-cycle-economyclockservice). A virtual pilot's completed flight goes through this exact same pipeline - see [Virtual pilots and wall-clock resolution](#virtual-pilots-and-wall-clock-resolution-virtualflightresolverservice). |
| `Flights/` | Flight tracking's pure domain logic: `FlightPhase` (the ten-phase enum), `FlightPhaseThresholds` (the speed/altitude/timing constants that drive phase transitions), `FlightPhaseStateMachine` (advances phase-by-phase from telemetry samples, captures OOOI and nullable touchdown rates, and restores itself from a stored event history), `LandingQualityCalculator` (runway-centreline deviation), `FlightIntegrityMonitor` (elevated simulation rate, slew, and corroborated position-jump detection), `PositionAcquisitionGate` (withholds telemetry at the pump until a position is vouched for by a second agreeing fix - see [Simulator connection and the telemetry pipeline](#simulator-connection-and-the-telemetry-pipeline)), and `AircraftTypeMatcher` (family-level, informational-only aircraft matching). See [Flight-phase detection and landing quality](#flight-phase-detection-and-landing-quality). |
| `Routes/` | `FlightNumberGenerator` — deterministic odd/even outbound/return flight-number pairing per airline, pure and side-effect free. |
| `Scheduling/` | `WeeklyOccurrenceCalculator` (pure date math: given a day-of-week and a time of day, finds the most recent occurrence at or before a given instant, or every occurrence in a window - the shared timing primitive both the resolver and the live operations map build on), `PilotScheduleValidator` (the aircraft-per-duty-day invariant - every entry for one pilot on one day must reference the same `FleetAircraftId`, checked and failed fast before anything else - plus geographic continuity, aircraft double-booking across pilots, minimum turnaround, pilot rest/duty-length limits, and refusing a reserved-for-player aircraft outright, all against a proposed set of schedule entries, cyclically across the week), `VirtualPilotPerformanceCalculator` (a pilot's weekly sectors/hours/revenue/cost summary shown on the Pilots page), and `PilotSkillCalculator` (derives `Pilot.SkillRating` from hours flown and idle time - see [Reputation and pilot skill](#reputation-and-pilot-skill)). Pure, no database. See [The weekly schedule builder](#the-weekly-schedule-builder-aircraft-per-duty-day). |
| `Money/` | `CurrencyCatalogue` (the supported-currency list, each with a fixed display rate against the GBP-pegged base unit) and `MoneyFormatter` (base-unit → display-currency conversion and formatting). See [Money is stored in a single base unit](#money-is-stored-in-a-single-base-unit). |
| `Planning/` | Route preview math: `GreatCircle` (distance/bearing), `CruiseAltitudeSelector`, `BlockTimeEstimator`, `BlockFuelEstimator` (also used by `TankeringAdvisor` to price the extra weight a tankered sector would burn), composed together by `RoutePreviewCalculator` into one preview result plus validation warnings - including the suggested fare, sourced from `Economy/ReferenceFareCalculator` - deliberately the *same* calculator the demand engine anchors its elasticity curve to, so the fare a player is offered and the fare the economy scores them against can never drift apart. These were once two separate hardcoded formulas that agreed only by luck; one source is the fix. Pure, deterministic, no I/O. |
| `Finance/` | `LoanCalculator` (amortising loan monthly-payment math and the monthly interest/principal split `EconomyClockService` applies), `LoanRateCalculator` (the only code allowed to price a loan's interest rate - risk-based, scaling from the playstyle's base rate to its cap as the loan consumes more of the airline's borrowing capacity), `LoanEligibilityCalculator` (borrowing capacity itself: 30% of trailing 30-day net operating cash flow), `AircraftDepreciationCalculator` (an owned aircraft's sale value - depreciates with condition, cycle wear, and a grounded-for-maintenance penalty), `LeaseTerminationCalculator` (pro-rata charge for the part of the current 30-day period used, plus a flat early-termination fee), and `LoanSettlementCalculator` (full-payoff quote with an early-settlement fee, and overpayment - which reduces principal and shortens the term without changing the monthly payment). The last three back `FleetDisposalEndpoints`/`FinanceEndpoints`'s quote-then-confirm actions - see [Getting rid of an aircraft, and settling a loan](#getting-rid-of-an-aircraft-and-settling-a-loan-the-quote-then-confirm-pattern). `LoanCalculator`/`LoanRateCalculator`/`LoanEligibilityCalculator` are used for both the startup loan at airline creation (now capped at a flat per-playstyle `EconomyConfig.Loan.MaxStartingLoanPrincipal`, since a brand-new airline's zero trading history makes the cash-flow-based mid-game cap resolve to zero) and a mid-game loan from the Fleet page. |
| `Fleet/` | `AircraftRepositionEvaluator` — which airports a stranded aircraft may be moved to (derived from the airline's own active routes, both directions), every refusal rule for the move, and the fee arithmetic. Pure and clock-free, so the rules are unit-testable with exact values; see [Repositioning a stranded aircraft](#repositioning-a-stranded-aircraft). |
| `Airlines/` | `AircraftRegistrationGenerator` (country-appropriate tail number generation for a newly leased aircraft). Starting capital, lease deposit and starting pilot salary used to live here as `AirlineCreationDefaults`; they now live in `Economy/EconomyConfig.AirlineStartup` instead, alongside every other tunable economy figure, so airline-creation balance can be retuned the same way as everything else - see the `Economy/` row above. |
| `Airports/` | `AirportSearchRanking` and `AirportSizeCategoryMapper`, used by airport search and by the home-base-suitability check in airline creation. |
| `AppPaths.cs` | Runtime resolution of every path FSOps writes to. See [App paths](#app-paths-no-hardcoded-filesystem-paths). |

## API endpoint surface

All REST endpoints are versioned under `/api/v1`:

| Route group | Endpoints | Covers |
|---|---|---|
| `/airline` | `POST`, `GET`, `PUT`, `GET /summary`, `GET /strategy-profiles`, `GET /playstyles`, `GET /ledger`, `DELETE` | Founding an airline (creates the airline, leases the starter fleet aircraft, hires the first pilot, and posts opening ledger entries in one transaction, resolved against the chosen `AirlinePlaystyle` — see [Playstyles and EconomyConfigCatalog](#playstyles-and-economyconfigcatalog)), fetching/updating its identity, accent colour and strategy profile (a strategy change is going-forward only — it never touches completed flights or existing routes' fares; playstyle has no update path at all, by design), a summary including derived cash balance and counts, the live per-profile fares/sensitivity/load-factor/cost figures and per-playstyle starting-capital/lease/insurance/loan-cap figures read straight from `economy-config.json` (backs the profile and playstyle pickers in onboarding and Settings), the itemised ledger newest-first (backend visibility only - no Finances page consumes this yet), and the "start over" soft-delete. |
| `/settings` | `GET`, `PUT`, `GET /currencies` | Per-user display settings — currency, units, theme — created lazily on first access, and the supported-currency catalogue. Accent colour and strategy profile live on the airline itself (`/airline`), not here, since they're airline-specific rather than account-wide. |
| `/routes` | `POST`, `GET`, `PUT /{id}`, `GET /{id}`, `DELETE /{id}`, `POST /{id}/return-leg`, `POST /preview` | Route creation, which always creates both directions of a round trip in one call (see [Round trips and where your aircraft actually is](guides/user-guide.md#round-trips-and-where-your-aircraft-actually-is)); listing (self-healing — backfills a missing return leg or flight number for legacy single-leg routes); updating a route's flight number, fare, or active flag; fetching or soft-deleting a route pair together; a manual return-leg repair tool for routes that couldn't be paired automatically; and the live, throw-free preview used while picking airports, including the tankering advisory (see [Persistent fuel and tankering](#persistent-fuel-and-tankering)). |
| `/airports` | search / lookup endpoints | Backing the airport pickers in route building and airline creation, using the imported world airport/runway data. |
| `/worlddata` | `GET /status` | Reports whether world data import has finished, is in progress, and how many airports/runways were loaded — polled by the frontend on first launch. |
| `/sim` | `GET /status` | The simulator's current connection state, which source is active (`SimConnect` or `Fake`), the aircraft title last seen, and when the last telemetry sample arrived — backs the top bar's sim indicator and the Fly screen's readiness checks. |
| `/fleet` | `GET`, `GET /aircraft-types`, `GET /registration-suggestion`, `POST /lease`, `POST /buy`, `GET /loans`, `GET /loan-eligibility`, `GET /loan-quote`, `POST /loans`, `PUT /{id}/reservation`, `PUT /{id}/registration` | The Fleet page's backing data (see [Maintenance scheduling](#maintenance-scheduling) for the grounding/condition/hours fields it returns, releasing any aircraft whose downtime has elapsed first) and every way to grow the fleet beyond the founding lease: leasing or buying (new or used) an additional aircraft, taking out a loan (`GET /loan-eligibility`/`GET /loan-quote` are read-only previews of the exact pipeline `POST /loans` uses), toggling an aircraft's reservation (`FleetEndpoints.SetReservationAsync` — see [Aircraft reservation](#aircraft-reservation-the-two-way-gate-and-reservationreconciler)), and renaming a registration. |
| `/fleet` (disposal) | `GET /{id}/sale-quote`, `POST /{id}/sell`, `GET /{id}/lease-termination-quote`, `POST /{id}/end-lease` | `FleetDisposalEndpoints` — selling an owned aircraft or ending a leased one's lease early, both behind a quote-then-confirm pair. See [Getting rid of an aircraft, and settling a loan](#getting-rid-of-an-aircraft-and-settling-a-loan-the-quote-then-confirm-pattern). |
| `/fleet` (repositioning) | `GET /{id}/reposition-options`, `POST /{id}/reposition` | `FleetRepositionEndpoints` — moving an idle aircraft to another airport the airline already serves without flying it there, behind the same quote-then-confirm pair as disposal. See [Repositioning a stranded aircraft](#repositioning-a-stranded-aircraft). |
| `/fleet` (maintenance) / `/away-summary` | `GET /{id}/maintenance-quote`, `POST /{id}/perform-maintenance`, `GET /away-summary`, `POST /away-summary/acknowledge` | `MaintenanceEndpoints` — bringing an A/C-check forward at the player's choosing (quote-then-confirm, same pattern as disposal), and the "while you were away" catch-up summary (`GET` is side-effect-free except for a one-time bootstrap; only `POST /acknowledge` ever advances the cursor). See [The Finances page and the away-summary](#the-finances-page-and-the-away-summary). |
| `/finance` | `GET /summary`, `GET /leases`, `GET /loans`, `GET /loans/{id}/settlement-quote`, `POST /loans/{id}/settle`, `POST /loans/{id}/overpay`, `GET /pilots`, `GET /costs`, `GET /routes`, `GET /ledger` | `FinanceEndpoints` — backs the Finances page in full: cash/period P&L, leases with real payment dates, loan settlement (quote-then-confirm) and overpayment, per-pilot revenue-vs-cost (partly estimated — see the method doc), fixed/variable cost split, profit-and-loss per route, and the filterable itemised ledger. Every figure not explicitly named an estimate is summed straight from posted `LedgerTransaction` rows at request time, never cached. See [The Finances page and the away-summary](#the-finances-page-and-the-away-summary). |
| `/flights` | `POST /start`, `POST /{id}/abandon`, `POST /{id}/complete-manual`, `GET /active`, `GET /{id}`, `GET`, `GET /options` | Starting a tracked flight (validates one-flight-at-a-time, resolves fleet aircraft and pilot, snapshots the plan, flags an aircraft-type mismatch if the sim's loaded aircraft doesn't match, and posts the fuel-uplift ledger line for that sector's trip/taxi/contingency fuel via `FlightEconomicsPoster.PostFuelUplift`); abandoning or manually completing a flight that needs resolution (manual completion also posts the sector's non-fuel economics, using the planned block time since nothing was actually measured); the currently active/interrupted flight with its live snapshot; a single flight's full detail including its event history and posted ledger lines; flight history; and, for the Fly screen, `GET /options`, which now lists **every** fleet aircraft physically at each route's departure airport (not just one candidate), each annotated with `isFlyable` and, when false, a specific `reason` (in flight, in maintenance until a stated time, another flight already in progress, or not reserved to the player). A live-tracked flight's own non-fuel economics post from `FlightLifecycleService` once it reaches Shutdown, not from this endpoint group; the player's own pilot now also accrues `HoursFlown` at completion, from both this path and the manual-completion path, via the shared `MaintenancePoster.PostFlightHours`. |
| `/pilots` | `GET`, `POST`, `DELETE /{id}`, `GET /{id}/schedule`, `PUT /{id}/schedule`, `POST /{id}/schedule/aircraft-options`, `POST /{id}/schedule/leg-options`, `GET /schedule/overview` | Hiring a virtual pilot (a name, or an auto-generated `First Officer N`; standard salary, skill rating fixed at 50, no upfront cost — see [Virtual pilots and wall-clock resolution](#virtual-pilots-and-wall-clock-resolution-virtualflightresolverservice)) and releasing one (blocked while they're mid-flight); reading and replacing a pilot's whole `PilotSchedule` in one call (validated by `PilotScheduleValidator` before anything is saved — a rejected save changes nothing); an aircraft-options endpoint for the builder's first step (which airframes are eligible for a given duty day, a reserved or grounded one marked ineligible with its reason) and a leg-options endpoint for its second (which legs a chosen aircraft can fly next); and `GET /schedule/overview`, the airline-wide, read-only, by-aircraft/by-pilot view backing the Pilots page's schedule overview. A player pilot (`Pilot.IsPlayer`) never owns a `PilotSchedule` — only a virtual pilot can. See [The weekly schedule builder](#the-weekly-schedule-builder-aircraft-per-duty-day). |
| `/operations` | `GET /live` | Backs the Dashboard's live operations map: the player's own in-progress flight (from live telemetry) plus every virtual pilot's currently-airborne scheduled occurrence (interpolated server-side from the schedule template and elapsed time — nothing extra is persisted for it), and the airline's route network for the map's background arcs. See [The live operations map](#the-live-operations-map). |

## Request and data flow

There are two flows through the system: user-driven requests from the browser, and live telemetry from the simulator.

**Browser-driven flow:**

```
Browser SPA (fsops-web)
    │
    ├── REST calls  ──────────────► /api/v1/*
    └── live updates ◄────────────► /hubs/live  (SignalR)
                                        │
                                        ▼
                              Backend services (FSOps.Server)
                                        │
                                        ▼
                              Domain / economy logic (FSOps.Core)
                                        │
                                        ▼
                              EF Core (FSOps.Data) ──► SQLite
```

The frontend talks to the backend two ways: versioned REST endpoints under `/api/v1` for requests with a clear request/response shape (creating an airline, adding a route, and so on), and a SignalR hub at `/hubs/live` for anything that needs to push to the browser without being asked — a heartbeat, live flight telemetry, and flight-completion/needs-resolution notifications.

**Simulator-driven flow:**

```
MSFS 2024
    │
    ▼
SimConnect  ──────────────► FSOps.Sim (telemetry channel)
                                        │
                                        ▼
                              Flight tracker (FSOps.Server / FSOps.Core)
                                        │
                                        ▼
                              Broadcast over /hubs/live ──► Browser SPA
```

`FSOps.Sim` owns the SimConnect connection and turns raw sim variables into a telemetry stream. `SimTelemetryService` (`FSOps.Server`) pumps that stream at the sim's own sampling rate, feeding `FlightLifecycleService` full-rate for accurate phase detection while throttling what it broadcasts over `/hubs/live` to twice a second, so the browser gets smooth live updates without every sim frame crossing the wire. `FlightLifecycleService` is where telemetry becomes domain logic — advancing the flight-phase state machine, capturing landing quality, and persisting the append-only event log a flight is built from (see [Flight-phase detection and landing quality](#flight-phase-detection-and-landing-quality) and [The append-only flight event log and crash recovery](#the-append-only-flight-event-log-and-crash-recovery)) — before broadcasting live updates back out over the same hub the browser is already listening on.

## Design principles

### Local-first, structured to move online later

FSOps runs entirely on your machine today — SQLite on disk, no account, no server to talk to. But it's structured so that moving parts of it online later (sync across machines, a hosted backend, multi-user features) is a matter of extension rather than rewrite:

- **GUID primary keys** throughout the domain model, rather than auto-incrementing integers, so records generated locally on different machines never collide.
- **UTC timestamps** everywhere, so time comparisons are never ambiguous once data starts moving between machines in different time zones.
- **Append-only event and ledger tables** (see below) rather than rows that get overwritten in place, which is what makes eventual sync or replay tractable.
- **A versioned API** (`/api/v1`) from day one, so the contract between frontend and backend can evolve without breaking older clients.
- **An auth abstraction** (`ICurrentUser` in `FSOps.Server`) that every endpoint and query is expected to depend on instead of reading identity directly off the request. Locally there's only ever one user, but this means introducing real authentication later is a dependency-injection change, not a rewrite of every endpoint.
- **Every row scoped by owner.** `OwnerUserId` on account-level records (`Airline`, `UserSettings`) and `AirlineId` on everything that hangs off an airline (routes, fleet, pilots, ledger transactions, flights) mean every query already filters by who owns what. Locally that's always the same single user, but the schema is already shaped for multiple users sharing one database once real auth lands.

### Money is stored in a single base unit

Every amount that touches money — `LedgerTransaction.Amount`, an aircraft's purchase price, a route's fare, a loan's principal — is stored in one fixed base currency unit (GBP-pegged; see `CurrencyCatalogue.BaseCurrencyCode` in `FSOps.Core.Money`), never in whatever currency the user happens to have selected.

Display currency is purely a read-time transform: `MoneyFormatter.ConvertFromBase` multiplies a base-unit amount by the selected currency's fixed `DisplayRate` and formats it with that currency's symbol and decimal places. Nothing is ever converted the other way and written back — a user switching their display currency in settings changes what every screen shows, never what's stored. This also means display rates don't need to be fetched or kept in sync with real exchange rates; FSOps is a game economy, not a forex simulator.

### Locale-pinned date formatting

`formatDate`/`formatDateTime` (`src/fsops-web/src/lib/format.ts`) pin every date shown in the UI to a fixed locale (`en-GB`, so a date reads "31 Aug 2026") rather than passing `undefined` to `toLocaleDateString` and letting it follow whichever locale the machine happens to be configured with. The same amount would otherwise render "31 Aug 2026" on one machine and "Aug 31, 2026" on another, purely as a byproduct of Windows' regional settings — inconsistent between players, and impossible to write a stable test assertion against. The numeric formatters in the same file (`formatMoney`, `formatDistance`, `formatAltitude`, `formatWeight`) already pinned `en-US` for their number grouping for the same reason; the date formatters bring dates in line. This is deliberately separate from the user's UTC/local-time and 24-hour-clock preferences (`UserSettings.TimeDisplay`/`Use24HourClock`) — those are real choices the player makes and are respected everywhere a time-of-day is shown; the locale pin only fixes the field *order and punctuation* of a calendar date, which was never a meaningful choice to begin with.

### Append-only ledger

Your airline's cash balance is never stored as a single mutable number that gets incremented and decremented in place. Instead, every financial event — a ticket sale, a fuel bill, a lease payment, a loan drawdown, starting capital — is written as its own row in an append-only `LedgerTransaction` table. The cash balance the API and UI show is always derived as **`SUM(LedgerTransaction.Amount)`** for the airline, computed at read time, rather than read off a stored column.

`FlightEvent` (the phase-by-phase record of a tracked flight) follows the same append-only rule for the same reason: a historical record that's only ever added to is self-auditing — nothing is silently overwritten, and any derived value can be reconstructed or verified independently just by re-reading the log. See [The append-only flight event log and crash recovery](#the-append-only-flight-event-log-and-crash-recovery) for how this is put to use.

This costs a bit of query overhead — SQLite's EF provider can't translate `Sum()` over `decimal` into SQL, so the (small, per-airline) set of amounts is pulled into memory and summed there — in exchange for a system where the financial history can't drift out of sync with how it got there.

### App paths: no hardcoded filesystem paths

FSOps installs into `Program Files`, which is read-only for standard users, so nothing may ever be written next to the executable. Every path FSOps writes to — the SQLite database, log files, and any future config — is resolved at runtime through `AppPaths` (`FSOps.Core.AppPaths`), which resolves everything under the current Windows user's `%LOCALAPPDATA%\FSOps\` and creates directories on first access:

- `AppPaths.DataDirectory` → `%LOCALAPPDATA%\FSOps\`
- `AppPaths.DatabasePath` → `%LOCALAPPDATA%\FSOps\fsops.db`
- `AppPaths.LogsDirectory` → `%LOCALAPPDATA%\FSOps\logs\`

No file path is ever hardcoded elsewhere in the codebase — everything goes through `AppPaths`. That's what makes FSOps work correctly wherever it's installed and for whichever Windows account is running it, without an installer needing to set permissions on a shared location.

### Deterministic, testable planning logic

Route planning — distance and bearing, cruise altitude selection, block time and fuel estimation, fare suggestion — lives in `FSOps.Core.Planning` with no dependency on the database, the web framework, or the simulator. Given the same inputs, it produces the same outputs every time. That determinism is what makes it practical to unit test thoroughly in `FSOps.Core.Tests`: planning behaviour can be verified in isolation, without spinning up a server or a database, and without MSFS anywhere in the loop. The economy engine (`FSOps.Core.Economy`) follows the same pattern — demand, fares, fuel pricing and flight costs are all pure functions of their inputs, with even fuel price's day-to-day drift coming from a hand-rolled stable hash rather than a stateful random number generator, so the exact same (airport, date) always prices identically. The one input that isn't fully pinned down by the caller is *when*: `DemandCalculator` reads the real clock (`DateTimeOffset.UtcNow`) for season and day-of-week, so the same route genuinely does price differently depending on what day you fly it — that's intentional (see [The economy simulation](guides/user-guide.md#the-economy-simulation) in the user guide), not a break in determinism, since every test pins the date explicitly rather than relying on the clock.

### Sim abstraction

`FSOps.Sim` sits behind `ISimSource` — a small interface (connection state, the currently loaded aircraft, and a channel of telemetry samples) that the rest of the app depends on, not any concrete implementation directly. Deliberately, no SimConnect-specific type is allowed to leak across that interface. That means a fake or replay-based telemetry source can stand in for MSFS entirely, which is what makes it possible to develop and test flight tracking, phase detection, and landing scoring without the simulator running at all, and without a human re-flying the same approach over and over to test a bounce-detection edge case.

`FakeSimSource` is that replay source: it plays back a scripted flight from a JSON file (the bundled default is a roughly 93-minute EGKK→LEBL sector) at a configurable time-compression factor, optionally looping. `SimConnectSource` is the real adapter, wrapping the `CTrue.FsConnect` library. Which one runs is a plain startup switch — the `sim` command-line argument (or `Sim:Source` in configuration) selects `fake` for the replay source; anything else, including nothing at all, talks to a real, running copy of MSFS. This is what the whole test suite and most day-to-day development runs against, rather than requiring MSFS to be open.

## Simulator connection and the telemetry pipeline

**Connecting.** `SimConnectSource` runs a connection loop for the lifetime of the process: each attempt creates a fresh `FsConnect` instance, calls `Connect()`, and only then registers FSOps' telemetry and aircraft-identity data definitions, before waiting for either a connected or disconnected signal. That ordering is deliberate and was previously a real bug: `RegisterDataDefinition` forwards straight to the underlying SimConnect handle, which doesn't exist until `Connect()` has actually run — registering first silently failed to reach a live MSFS 2024 session. The fix (registering after connecting, not before) has been verified against live MSFS 2024 with a Fenix A320. If a connection attempt fails or drops, it's disposed and retried after a fixed interval (5 seconds). This is why FSOps doesn't need restarting just because MSFS wasn't ready the first time — it keeps trying on its own for as long as it's running.

**Sampling rate.** Telemetry is read at an adaptive rate rather than a fixed one: roughly 5 Hz in normal flight, stepping up to essentially every sim frame once the aircraft is below 2,000 ft AGL, where the extra fidelity matters for accurately catching the moment of touchdown. A small hysteresis band (300 ft) around that threshold stops the rate from flapping back and forth near the boundary.

**What's read.** Each sample carries position (latitude/longitude), altitude (both MSL and AGL), indicated airspeed, ground speed, vertical speed, true and magnetic heading, on-ground state, engine-running state, parking brake state, G-force, the instantaneous touchdown vertical velocity simvar (only meaningful at the moment of ground contact — this is what landing-rate scoring is built on), and total fuel weight. The aircraft's title and ATC model/type are requested separately from telemetry — on connect, and again on every `AircraftLoaded` event — and, unlike every other field, that request is **retried every few seconds for as long as it keeps coming back empty**. FSOps typically connects while MSFS is still sitting at the main menu with no aircraft loaded, so a single one-shot request (the original design) routinely landed before there was anything to answer it, leaving the title blank for the rest of the connection with no second chance — which silently disabled family-level aircraft-type matching, since a mismatch can only be flagged once there's a title to compare. The retry (`SimConnectSource.RetryAircraftIdentityIfUnknown`) stops the instant a title arrives and otherwise costs one cheap request per sample-driven check.

**Pushing it live.** `SimTelemetryService` (`FSOps.Server`) reads the full-rate sample stream and does two things with it: it hands every sample to `FlightLifecycleService` for phase detection (see below), and it broadcasts a throttled subset — at most once every 500 ms (2 Hz) — over the `/hubs/live` SignalR hub as a `telemetry` message, carrying timestamp, position, altitude, speeds, headings, on-ground state, and connection state. The throttling keeps the browser's live view smooth without pushing every sim frame across the wire; the full-rate stream still reaches the phase state machine so a fast transition (like the instant of touchdown) is never missed just because the broadcast rate is lower.

**Position acquisition: nothing is handed out until a second fix agrees with it.** `SimTelemetryService` is the single reader of the sim source's channel, which makes it the single place every position-consuming feature in the app is fed from — the phase state machine, the live map, the SignalR broadcast, VATSIM corroboration, and the flight's own recorded track. That's also the one place a bad opening fix can be stopped before it reaches any of them. SimConnect can deliver a packet before the sim has a real aircraft state to put in it: the first real flight ever tracked with FSOps opened with a fix at roughly 0.0N 90.0E (the Bay of Bengal) — 5,505 nm from the stand the aircraft was actually parked on — and that single reading was enough on its own to void a clean two-hour sector once it reached the integrity monitor (see below). `PositionAcquisitionGate` (`FSOps.Core.Flights`) now sits at the pump and withholds every sample — on first connection, and again on every reconnect — until a second fix agrees with it (the aircraft could plausibly have covered the distance between the two at the reported simulation rate); the unvouched first fix is discarded, and the corroborated fix is the first position anything downstream ever sees. This fails **open**, not closed: a sim that goes on reporting nonsense indefinitely doesn't produce a silent telemetry blackout — the gate gives up after twenty seconds and lets everything through regardless, with a warning logged, because a total loss of telemetry is a worse failure than one uncorroborated reading. Once a position is acquired the gate stands aside permanently for that connection; nothing about the middle of a flight (phase changes, touchdown capture, OOOI) behaves any differently than before this existed.

A reconnect is detected by subscribing to the sim source's own connection-state change, not by inspecting the connection state once per sample. Sampling it per sample cannot see an outage at all: no telemetry arrives while the link is down, so by the time the next sample turns up the state has already walked Connected → Disconnected → Connected and reads exactly as it did before. The same signal is republished as `SimTelemetryService.TelemetryInterrupted`, which is what tells the active flight's integrity monitor that its timeline has a hole in it (see [Position-jump detection](#flight-phase-detection-and-landing-quality) below) — anything downstream that reasons about one sample *relative to the one before it* has to know, and rebuilding the gate while telling nothing else was itself enough to void a clean sector.

`GET /api/v1/sim/status` exposes the same connection state on demand — current state, which source is active, the last-seen aircraft title, and the last sample's timestamp — for anything that wants to poll rather than subscribe (the Fly screen's readiness checks use this, alongside the live hub).

## Flight-phase detection and landing quality

A tracked flight moves through ten phases, in order, with one deliberate exception: **Preflight → Taxi out → Takeoff roll → Climb → Cruise → Descent → Approach → Landed → Taxi in → Shutdown**, except that a go-around sends the state machine back from Landed to Climb rather than continuing forward. `FlightPhaseStateMachine.Advance()` runs this transition logic once per telemetry sample, driven entirely by thresholds in `FlightPhaseThresholds` — ground speed crossing 2 kt or 40 kt, vertical speed settling within a 300 fpm band for 20 seconds (levelling into cruise) or descending past -300 fpm for 15 seconds (starting a descent), altitude dropping below 3,000 ft AGL while descending (entering approach), and so on. All of this is timed from the samples' own timestamps, never wall-clock time, which is what makes the whole thing replayable and deterministic in tests.

OOOI times (out/off/on/in) are captured as by-products of specific transitions: **out** when taxi begins, **off** when the aircraft becomes airborne, **on** at the first ground-contact after the approach, **in** once engines are off and the parking brake is set at the end of taxi-in.

**Touchdown and landing quality.** Every ground-contact event while approaching or already landed registers a touchdown record: a touchdown rate (feet per minute) and G-force, both tracked as a peak over the few seconds immediately following contact rather than read once at the instant of contact, so a hard bounce a beat after touchdown is still captured. The touchdown rate is nullable end to end — `TouchdownRecord.Fpm` (`FSOps.Core.Flights`) is `double?`, not `double` — because the sim publishes its own touchdown simvar (`PLANE TOUCHDOWN NORMAL VELOCITY`) a beat *after* the wheels are actually down, and reading it at the instant of contact sees it still sitting at its idle zero: a real 59 fpm greaser was once recorded and shown as a confident 0 fpm this way. `FlightPhaseStateMachine.ResolveTouchdownRate` now watches the capture window rather than reading once: it prefers the sim's own simvar the moment it reports anything nonzero, falls back to the vertical speed from the last airborne sample before contact if the simvar never does (the same physical quantity a frame earlier, but not slope-corrected, so used only as a last resort), and — if neither is available — leaves `Fpm` as `null` rather than defaulting it to zero. `TouchdownRateSource` records which of the three happened, and every caller downstream (the report card, the Stats pilot logbook) is required to render a `null` rate as **"not measured"**, never as a false perfect landing. A landing that bounces (leaves the ground only briefly before settling) keeps accumulating touchdown records against the same landing; one that stays airborne for more than a few seconds is reclassified as a go-around instead, clearing the touchdown records and sending the phase machine back to Climb. Centreline deviation is computed separately, once a flight finalises: `LandingQualityCalculator` picks the runway at the arrival airport whose heading is closest to the touchdown's track, then measures the perpendicular distance from the touchdown point to that runway's centreline.

**Position-jump detection is corroborated, not judged on a single sample.** `FlightIntegrityMonitor.Observe` (`FSOps.Core.Flights`) used to flag a sector the moment one telemetry sample implied a speed above `ImpossibleGroundSpeedKt` (1,200 kt) relative to the sample before it — and a single uninitialised SimConnect fix (see the Bay-of-Bengal example above) was enough to trip that on its own and void a flight that was flown cleanly end to end. The governing rule is that an implausible A→B transition, on its own, says only that *one* of A and B is wrong and never which; and that because this flag **gates payment**, it must fail open rather than closed. A cheat that occasionally gets away with it is a far smaller harm than a pilot losing a sector they actually flew.

Several independent guards now stand in front of the flag. Each one exists because it closes a shape of bad data that was proven, by test, to void an honestly-flown sector:

- **Corroboration either side.** An implausible transition only **opens a suspicion**, and only once the aircraft has already been tracked through a minute (`CorroborationDwell`) of plausible movement beforehand — proving the position it supposedly left really was occupied, which a bad opening fix can never establish. The suspicion is **confirmed** once the aircraft goes on flying plausibly for another minute, and **dismissed** if it comes back close to where it appeared to leave from (`ReturnToOriginFraction`, within 25% of the jump's own distance). Confirming on "flew on normally" rather than on the next transition being plausible is what still catches a *sustained* reposition, since slewing at speed produces a whole run of impossible transitions rather than one.
- **Coming home revokes a confirmed jump.** `PositionJumpDetected` is deliberately **not** a one-way latch. Confirmation necessarily fires *mid-excursion* — a minute after the jump, while a bad reading is very often still being reported — so it cannot be the last word: a sector's fate would otherwise depend on the sim happening to right itself within sixty seconds of going wrong, and an aircraft that had been sitting on its stand for a couple of minutes (the ordinary way a sector begins) had enough corroboration behind it to be condemned by a single sustained glitch. If the aircraft later comes back to where a confirmed jump started from, under the same proximity rule that governs dismissal, that jump is revoked — by the same logic that justifies dismissal in the first place: an aircraft that ends up back where it started has flown nothing and gained nothing. Each confirmed jump is tracked and revoked in its own right, so returning from one excursion never clears a different one the aircraft stayed away from. Coming back is the **only** thing that revokes: not time, not distance flown, not a reconnect, not the flight ending. A teleport that is flown on from, or parked at, stays flagged — which is why revocation was chosen over the alternative of deferring confirmation until an excursion is "left behind", since a cheat who teleports to the destination and shuts down never leaves anything behind. `SlewDetected` is untouched by any of this and remains permanently one-way.
- **The opening fix.** When the flight's expected departure position is known, an opening fix implausibly far from it (`StartingFixToleranceNm`, 100 nm) is discarded outright rather than treated as either end of a jump — bounded by an acquisition window (`StartingFixAcquisitionWindow`, 60 s) so a player who genuinely starts tracking somewhere else doesn't have every sample discarded for the whole sector.
- **A jump has to be worth something.** An excursion whose total displacement never exceeds `NegligibleJumpDistanceNm` (5 nm) is never treated as a teleport, however absurd the speed it implies. At the sim's sampling rate even a two-mile correction — the size of error an ordinary scenery load or ground settle produces — implies tens of thousands of knots, so the speed test alone cannot separate bad data from repositioning. Nobody gains anything by teleporting five miles, and a move that gains nothing must never cost a sector. It is measured against the excursion's *total* displacement rather than any single sample-to-sample hop, so a reposition delivered as a run of small steps is still measured as the one large move it is.
- **A correction back to the departure airport is not a cheat.** An implausible transition that lands the aircraft within `DepartureCorrectionRadiusNm` (10 nm) of the airport this flight is departing from, before it has left, is read as a bad reading being corrected — nobody teleports *to* the stand they are supposed to be standing on. The exemption is strictly one-way: it is spent permanently the moment the aircraft is seen to get airborne or to be flown out of that radius, and can never be re-entered, because landing back where you departed is itself a legitimate paid outcome. "Seen to" is load-bearing — being *reported* somewhere is not the same as having gone there, so the check is on the aircraft's **displacement** from the start of the current unbroken run of telemetry, never on a running total of per-sample distances (a reading that sits still while carrying ordinary noise racks up unlimited distance while going nowhere) and never across a transition the monitor has already judged impossible.
- **Speed cannot be inferred across a gap.** `FlightIntegrityMonitor.NotifyTelemetryInterrupted` drops the previous sample and the corroboration behind it when the sim link goes away and comes back, because the samples either side of an outage are not consecutive observations of the same continuous motion. This matters because a reconnecting SimConnect commonly replays the position it last had for a while, which bridges the hole and leaves the outage invisible in the timestamps — and by the time the true position arrives the aircraft has genuinely flown on for however long the link was down. Until the resumed feed is seen to actually move, an unchanging position rebuilds no corroboration, since that is exactly what a stalled feed looks like; and jitter, being nothing but implausible transitions, cannot certify the link as recovered either. Everything already *found* — slew, an elevated rate, a confirmed jump, an outstanding suspicion — deliberately survives a reconnect, so dropping the link can never launder evidence. A long gap with no reconnect behind it is **not** treated this way: when samples simply stop and restart the timestamps stay honest, so the implied speed remains a true lower bound and an honest flight cannot trip it.
- **Time acceleration is normalised by the greater of the two reported rates.** Exactly one transition per rate change straddles two different rates, and the ground it covers was flown at one of them, not at something in between. Averaging the two under-divides that sample towards twice the aircraft's true ground speed, which condemned anything much above 600 kt over the ground on a single large step (1x straight to 64x or 128x, as a bound "set rate" produces) — an airliner in a strong jet stream, not an exotic case. Taking the maximum can never under-divide, and costs nothing against cheating: a jump of hundreds of miles in a fraction of a second still implies tens of thousands of knots after being divided by 128.

`PositionAcquisitionGate` above and this corroboration logic are deliberately two separate defences at two different layers — the gate gets a wildly-wrong fix out of the pipeline before anything sees it at all; this monitor is what still stands guard mid-flight, where the gate has already stood aside for good. Neither is sufficient alone, and the gate specifically cannot help with a *stationary* bad fix, because two identical packets agree with each other. `SlewDetected` (from the sim's own slew simvar) needs no corroboration at all and is unaffected by any of this.

**Aircraft-type matching.** `AircraftTypeMatcher` checks the sim's reported aircraft title and ATC model against a route's expected aircraft type using a stored list of regular expressions per family (so a 737-800 and a 737-700 both match a "B737 family" pattern without special-cased logic — the permissiveness lives in the stored pattern, not the matcher). This check is deliberately incapable of ever blocking or costing anything: an unparsable pattern list or a broken regex is treated as "no match" rather than as an error, and the result is stored purely as an informational flag on the flight, never consulted by anything that touches money.

**Backfilled at completion, for a flight that started before an identity was known.** `Flight.TitleFlown` is normally set at `POST /flights/start`, but FSOps often connects while MSFS is still at the main menu — before the identity retry described above (see [Simulator connection and the telemetry pipeline](#simulator-connection-and-the-telemetry-pipeline)) has anything to report yet — so a flight can genuinely start with `TitleFlown` still empty. `FlightLifecycleService.FinalizeFlightAsync` checks for exactly this at completion: if `TitleFlown` is still empty but the tracker has since observed a real title (`ActiveFlightTracker.LastAircraftTitle`, updated on every sample — see above), it backfills `TitleFlown` and runs the type-mismatch check retroactively at that point, rather than leaving both permanently blank. Before this existed, an empty `TitleFlown` and a `null TypeMismatch` looked exactly like "the check ran and found a match" rather than "the check never ran at all" — the type-mismatch badge had, in practice, almost never fired for anyone.

## The append-only flight event log and crash recovery

Every meaningful thing that happens during a tracked flight — a phase change, a touchdown, a periodic position snapshot (every 15 seconds, so a flight's track can be reconstructed even between phase changes), an aircraft-type mismatch — is written as its own row to the `FlightEvent` table, following the same append-only rule as the financial ledger (see [Append-only ledger](#append-only-ledger)): never updated, never deleted. `FlightLifecycleService` queues these events in memory and writes them off the telemetry hot path in small batches, so persisting to SQLite never competes with processing the next incoming sample.

This log is what makes a tracked flight survive FSOps or MSFS crashing partway through. On startup, if a flight is still marked in progress, `FlightLifecycleService` loads its full event history and replays it through `FlightPhaseStateMachine.RestoreFrom()` — reconstructing phase, OOOI times, and any touchdowns already recorded, purely from the stored events, with no reliance on any in-memory state that didn't survive the crash. It then waits for the simulator to reconnect near the flight's last known position (within about 5 nm) for up to 30 seconds. If that happens, tracking resumes as if nothing happened. If it doesn't, the flight is marked **Interrupted** rather than silently resumed or abandoned — surfaced to the user as a flight that needs resolving (see [Ending a flight, and what happens if it gets interrupted](guides/user-guide.md#ending-a-flight-and-what-happens-if-it-gets-interrupted) in the user guide), where they can wait for reconnection, complete the flight with estimated figures, or abandon it outright. Nothing about a flight's already-recorded history is ever lost by this process — it's exactly what the replay is built from.

## Playstyles and EconomyConfigCatalog

Every airline is created with a permanent `AirlinePlaystyle` (`Casual` or `TrueLife` - see [Playstyles](guides/user-guide.md#playstyles) in the user guide) chosen once at founding, with no update path afterwards. `EconomyConfigCatalog` is what makes this work without branching logic scattered through the economy engine: at startup it parses `economy-config.json` once, reads the file's shared base fields (fares, demand, fuel, the maintenance cycle, the used-aircraft market - identical for every airline) plus two small `casual`/`trueLife` override blocks (starting capital, lease deposit term, every aircraft type's lease rate, monthly insurance, maintenance downtime, and the loan rate base/cap), and resolves two fully-merged `EconomyConfig` instances, validating each the same way a single config would validate itself.

A playstyle is a **named set of overrides, not a code path** - nothing downstream ever asks "is this airline Casual or True-life?" Callers resolve the right `EconomyConfig` once (`economyConfigCatalog.Get(airline.Playstyle)`, typically right after loading the airline that owns the request) and hand it to the same economy engine every airline uses. This is also why `Program.cs` deliberately does **not** register a flat `EconomyConfig` singleton in DI - one existed briefly during development and was removed on purpose: every config-derived figure must be resolved per-airline through the catalogue, and a constructor that fails to resolve one is meant to fail loudly (inject `EconomyConfigCatalog` instead) rather than silently fall back to a config that might be the wrong playstyle for the airline in scope.

## The monthly billing cycle: EconomyClockService

`EconomyClockService` is a `BackgroundService` that posts the fixed monthly costs a flight-by-flight economy never touches: one lease payment per active `Lease`, one salary line per `Pilot`, one insurance line per fleet aircraft (looked up live from `EconomyConfigCatalog` against the owning airline's playstyle, since insurance is the one figure that isn't already baked into a stored row), and the amortised interest/principal split (`LoanCalculator.ApplyMonthlyPayment`) for every outstanding `Loan`. It runs once immediately at startup - so a catch-up after being closed happens right away rather than up to a minute later - and then every 60 seconds after that.

A "month" is exactly 30 days of wall-clock time from `EconomyState.LastProcessedUtc` (a single-row watermark), not a calendar month. Each pass advances the watermark one 30-day period at a time, posting that period's charges and moving the watermark past it in the *same* `SaveChangesAsync` call - so a period's ledger rows and the watermark that says "this period is done" either both land or neither does; a crash mid-catch-up can lose at most the not-yet-committed remainder of that pass, never post a half-charged or double-charged month. A `SemaphoreSlim` also serialises overlapping calls within the process, the only place two "ticks" could ever race.

Two integrity properties matter here, both deliberate:

- **Catch-up is bounded.** If FSOps was closed for a long time, one pass posts at most 24 periods (about two years) before yielding back to the next 60-second tick - an unbounded closure doesn't mint an unbounded burst of charges in a single pass, but it does still catch up fully within a handful of passes.
- **The watermark only ever moves forward, and never past the current clock reading.** A clock reading that comes back *before* the watermark (a wound-back system clock) is treated as a backwards jump: nothing is processed, the watermark doesn't move, and it's logged rather than silently absorbed. Winding the clock *forward* can't mint more than the 24-period cap per pass either. This is what makes the wall-clock model resistant to clock manipulation in either direction.

## Fuel billing

A sector is billed for **what it actually burned**, at the departure airport's price — never a flat planned figure when the sim was watching, and never a credit for fuel left in the tank. `FSOps.Core.Flights.FuelBurnResolver` is the pure core: `Measure` turns a tracker's raw observations into one "what was burned" figure (or `null`), and `Resolve` decides whether that figure is trustworthy enough to bill or should fall back.

**Measuring a burn** (`Measure`, three tiers):

1. **Engines were observed running** at some point (`EngineStartFuelKg` set on `ActiveFlightTracker`) — returns `AccumulatedBurnKg`: a running sum of every fuel **decrease** seen *while the engines were running*, from the first sample where they came alive. **Gated in both directions**, and that symmetry is the whole design: a **rise** at any time (the load MSFS spawns you with, a menu fuel set, a GSX uplift, a mid-sector top-up) contributes nothing, and a **decrease with the engines off** (a defuel or ground-crew activity during a turnaround) contributes nothing either. The reasoning cuts both ways — no engines means no burn, so a reading that moves down with them off is not fuel consumed and must not be charged. `LastFuelKg` updates unconditionally on every sample regardless of engine state, which is what lets a shutdown and restart mid-sector resume seamlessly without a second baseline.
2. **Engines never observed running** (a sim that connected late, or a telemetry gap) — falls back to a plain `FirstSampleFuelKg` minus `LastFuelKg` subtraction.
3. **No telemetry at all** — `null`.

**Resolving what to bill** (`Resolve(measuredBurnKg, plausibilityCeilingKg, fallbackKg)`): a figure is trusted only when positive and within 3× the ceiling (in practice the sector's own `FuelBreakdown.ChargedFuelKg`); otherwise the fallback is used. Ceiling and fallback are deliberately separate parameters, because they differ by caller: a completed sector (`FinalizeFlightAsync`, `CompleteManualAsync`, `VirtualFlightResolverService`) falls back to its **full planned charge**, while an abandoned flight falls back to **zero** — so abandoning before the engines ever start bills nothing at all.

**Abandoning still costs.** `AbandonAsync` bills the burn accumulated up to that point rather than nothing, because the aircraft really did consume it, and because "escaping a bad sector must cost something" is a design rule the sunk fuel cost previously enforced.

**Where it posts:** `FlightEconomicsPoster.PostFuelBurn` writes the `Fuel` ledger line at the departure airport's price, **unconditionally with respect to payability** — a slewed or position-jumped sector earns nothing but still pays for the fuel it burned. `FleetAircraft.FuelOnBoardKg` survives as purely informational state, synced from telemetry and never billed.

**Removed with this model:** `FuelUpliftDetector`, `GroundFuelChangeKind`, `TankeringAdvisor`, and `BlockFuelEstimator`'s cost-of-carry mechanism, which existed solely to counterweight tankering. Tankering is no longer a mechanic: fuel cannot be bought cheap at one airport and carried to another, because carrying it is never what you pay for. Per-airport and regional price variation is deliberately **kept** — departing an expensive airport still costs more than a cheap one, which continues to shape hub choice; only the arbitrage disappeared.

## Maintenance scheduling

`MaintenanceScheduler` (`FSOps.Core.Economy`) is pure arithmetic: given a fleet aircraft's current hours/condition and the flight hours it has just flown, it decides whether an A-check (every 500 airframe hours) or a C-check (every 4,000) fires, what it costs, how long it grounds the aircraft, and the new hours/condition either way. Condition decays a small, configured amount per flight hour; an A-check partially restores it (35 points, capped at 100) while a C-check always resets it to 100 outright, since the shipped config makes the C-check interval a whole multiple of the A-check interval, so a C-check-due moment is always also an A-check-due moment and the two cycles reset together rather than scheduling a redundant A-check moments later. Downtime hours are the one maintenance figure that differs by playstyle (Casual: a handful of hours for an A-check, about a day for a C-check; True-life: about a day, and about a fortnight, respectively) - everything else about the cycle (interval hours, cost, condition decay/restore) is identical for both.

`MaintenancePoster` (`FSOps.Server.Services`) is the single call site both flight-completion paths (the live telemetry path in `FlightLifecycleService` and the manual-completion path in `FlightEndpoints`) use to apply the scheduler's result: it bumps `AirframeHours`, and if a check triggered, grounds the aircraft (`FleetAircraftStatus.InMaintenance`, with `GroundedUntilUtc` set so the UI can say not just "in maintenance" but until when), writes a `MaintenanceEvent` row, and posts the cost as a `LedgerTransaction`. `MaintenanceReleaser` is the wall-clock counterpart: rather than a dedicated background timer, it's called lazily at the top of every endpoint that reports or depends on fleet availability (`GET /fleet`, `GET /flights/options`, `POST /flights/start`) and releases any aircraft whose `GroundedUntilUtc` has already passed, so the very next request after a grounding period ends always sees it correctly released.

Buying a **used** aircraft (`FleetEndpoints.BuyAsync`) uses the same scheduler, via `MaintenanceScheduler.ResolveUsedAircraftState`: a used airframe costs 55% of the new price but starts 70% of the way into both maintenance cycles and at 70% condition rather than 100% - the discount is repaid through an earlier trip to maintenance, not given away for free.

## Virtual pilots and wall-clock resolution: VirtualFlightResolverService

`VirtualFlightResolverService` is a second `BackgroundService`, alongside `EconomyClockService`, that resolves every virtual pilot's scheduled occurrence whose block time has elapsed on the real wall clock. It runs once immediately at startup and then every 60 seconds, deliberately reusing `EconomyClockService`'s idempotency/monotonicity/capped-catch-up shape rather than inventing a second pattern:

- **Idempotency is structural.** Each occurrence's `Flight` row (and whatever `LedgerTransaction` rows it produces) and the `EconomyState.LastScheduleResolvedUtc` watermark advancing past that occurrence commit in the same `SaveChangesAsync` call, occurrence by occurrence — a crash mid-catch-up loses at most the not-yet-committed remainder of the pass, never posts an occurrence twice and never silently drops one. `LastScheduleResolvedUtc` shares the same `EconomyState` singleton row `EconomyClockService` owns rather than a second watermark row; only `EconomyClockService` ever creates that row, so if this service finds it missing it does nothing that pass and picks it up on the next tick.
- **Strictly monotonic**, exactly like the monthly billing watermark: the resolved-up-to point only ever moves forward and never past the current clock reading; a clock reading before it is a backwards jump — nothing processed, watermark unmoved, logged rather than absorbed.
- **Capped catch-up, bounded two ways per pass**: at most 500 occurrences actually resolved, and the enumeration window itself never looks further ahead than about 400 days — so neither a huge schedule backlog nor an enormous apparent clock jump can produce an unbounded burst of completed flights in one pass. The remainder simply continues on the next 60-second tick.

**Resolving one occurrence.** For each pilot's schedule entry due since the watermark, the service first decides whether it's actually flyable — the assigned `FleetAircraft` must be `Active` (not `InMaintenance` or `InFlight`) and physically at the route's departure airport (a grounding whose downtime has already elapsed by the occurrence's own departure time is released first, same lazy-release principle as `MaintenanceReleaser`). If it isn't flyable **specifically because the aircraft is grounded for maintenance**, and the schedule's `AutoSuspendOnMaintenance` flag is set (the default), the occurrence is recorded as `FlightStatus.Suspended` instead — no ledger line under either playstyle, since the schedule isn't at fault for a check the simulation imposed. Resumption needs no code of its own: the very next occurrence for that schedule simply re-evaluates the aircraft's status again, and once it's released the occurrence resolves normally. Every other unflyable reason (wrong airport, still airborne), or a maintenance grounding with `AutoSuspendOnMaintenance` off, falls through to the ordinary path: the occurrence is recorded rather than silently dropped or teleported, and `EconomyConfig.UnflyableSchedule.CancellationFee` decides whether it's logged as **Skipped** (fee is zero — Casual) or **Cancelled** (fee is charged and posted as a `CancellationFee` ledger line — True-life), with the specific reason stored on the `Flight` row (`UnflyableReason`) exactly as it will read in the UI (for example, `"G-OLAF is still at EGPF from an earlier leg — this leg needs it at EGGD."`). The aircraft's position is never touched for an unflyable occurrence, suspended or otherwise.

If it is flyable, the occurrence is flown as a **full economic citizen** through the exact same `FlightEconomicsCalculator` → `FlightEconomicsPoster` → `MaintenancePoster` pipeline a player's manually-completed flight uses (`FlightEndpoints.CompleteManualAsync` is this code's closest relative, and is followed wherever the two overlap): it earns ticket revenue, pays every itemised cost including the pilot's salary share, advances the aircraft's airframe hours and location, wears its condition, and can trigger a maintenance grounding exactly like a player flight would. Landing quality and delay variance come from the pilot's `SkillRating` via the seeded PRNG rather than measured telemetry, and no aircraft-type mismatch check applies (there's no simulator loaded aircraft to compare against) — otherwise it is indistinguishable from a player-flown sector in the ledger and the flight record. **Player flights are never touched by this service** — it only ever reads `PilotScheduleEntry` rows, which a player pilot (`Pilot.IsPlayer`) can never own (enforced in `PilotEndpoints.SaveScheduleAsync`).

## Reputation and pilot skill

`Airline.ReputationScore` and `Pilot.SkillRating` are both live inputs — read by the demand and virtual-pilot-performance code respectively — that used to be written once at creation (50) and never mutated again. Each now has a pure, recomputable engine in `FSOps.Core`, and a single poster that every relevant call site shares, following the same "one call point, not three-plus copies" reasoning already established for `MaintenancePoster` (see [Maintenance scheduling](#maintenance-scheduling)).

**`ReputationCalculator` (`FSOps.Core.Economy`)** advances a score by exactly one flight's outcome via exponential smoothing toward a per-flight target, rather than recomputing from full history on every call:

- `AdvanceForCompletedFlight(currentScore, config, delayMinutes, landingFpm)` blends an on-time score (100 within a grace tolerance of 5 minutes, decaying linearly to 0 by 45 minutes late) and a landing score (mapped from the same best/worst-case fpm bounds `VirtualPilotPerformanceCalculator` uses, so a player's real touchdown and a virtual pilot's simulated one are judged on literally the same scale) at an 0.8/0.2 weight, then steps the current score toward that blended target by a fixed smoothing factor (`Alpha ≈ 0.013767`, derived so a run of maximum-scoring sectors crosses 75 at exactly the 50th sector). Either input can be `null` (an elevated simulation rate, or no captured touchdown) and is excluded from the blend rather than guessed at; if both are `null` there is nothing honest to score and the score is returned unchanged.
- `AdvanceForCancelledOrSkipped(currentScore, config)` steps toward a harsher target (0) at a steeper multiplier (2.5×) than any completed-sector outcome could produce, so a cancellation always costs more standing than merely running late — deliberately never called for `FlightStatus.Suspended`, which is not this calculator's business to know about at all; every caller is responsible for that exclusion.
- `AdvanceForUnverifiedManualCompletion(currentScore, config)` takes **no** timing or landing input at all, stepping toward a flat target at a small multiplier (1/3 of `Alpha`, ≈ −0.23 from a reputation of 50, against an ordinary sector's own worst-case −0.69). An earlier version of this path derived an on-time score from wall-clock time between start and manual completion — that was a real, shipped exploit, and is worth naming so it's never retried: a flight completed moments after starting reads as an enormous *early* arrival under that formula (the wall clock there measures how fast the player clicked a button, not how the sector went), which scored as a perfect sector and turned "start, immediately complete, repeat" into a reputation-and-revenue farm.

**`ReputationPoster` (`FSOps.Server.Services`)** is the shared call point — `PostCompletedFlight`, `PostCancelledOrSkipped`, `PostUnverifiedManualCompletion` — used by all three completion paths: `FlightLifecycleService`'s real-telemetry completion, `FlightEndpoints.CompleteManualAsync`, `FlightEndpoints.AbandonAsync` (which reuses `PostCancelledOrSkipped` unchanged — an abandoned sector never happened from a passenger's point of view, exactly like a skipped or cancelled one, and its lost ticket revenue/fuel is a separate financial consequence, never a substitute for the reputation cost), and `VirtualFlightResolverService`'s flown/unflyable paths. It is never called for `FlightStatus.Suspended` anywhere. Each call mutates the same tracked `Airline` instance the caller's own flight/watermark commit already saves in one `SaveChangesAsync`, so the update inherits that commit's existing idempotency guarantee (`Flight.RevenuePosted` for a player flight, the per-occurrence Flight-row/watermark commit for a virtual one) rather than needing one of its own.

**Every exclusion was checked in both directions.** Building this surfaced a durable lesson: a rule that excludes a flight from reputation must be checked for whether it can be used to *avoid a loss*, not only whether it can *manufacture a gain* — the first question is the obvious one, the second is where the exploits actually were (the manual-completion timing bug above, and the "just abandon it" escape hatch both came from this). Any future change to what's excluded from reputation should be re-checked the same way.

**`PilotSkillCalculator` (`FSOps.Core.Scheduling`)** derives `Pilot.SkillRating` from career hours flown and idle time since last flown — fully recomputed on every call from `(hoursFlown, lastFlewUtc, now)`, never incremented, so calling it any number of times for the same inputs can never move a pilot's skill twice:

- **Growth**: `1 - 0.5^(hoursFlown / GrowthHalfLifeHours)` fraction of the way from `StartingSkill` (50) to `SkillCap` (87, deliberately below perfect so variance never fully disappears) — a diminishing-returns curve with a 300-hour half-life, applied identically to every pilot including the player (harmless, since a player's own flights are always judged by real telemetry, never by this rating).
- **Idle decay**: only ever triggered by a nonzero gap between `lastFlewUtc` and `now`. `IdleGracePeriodHours` (336h = 14 days) must elapse with no flight before decay starts at all; beyond that, a half-life decay (`IdleDecayHalfLifeHours` = 720h = 30 days) pulls the rating back down toward `StartingSkill` — never below it. `VirtualFlightResolverService`'s periodic pass is the **only** caller that ever passes a nonzero idle gap, and it deliberately skips every `Pilot.IsPlayer` pilot — the player's own record never decays, by construction, not by a guard bolted on afterward. A pilot on a standing schedule keeps flying on the wall clock even while FSOps is closed (see [Virtual pilots and wall-clock resolution](#virtual-pilots-and-wall-clock-resolution-virtualflightresolverservice)), which keeps `LastFlewUtc` fresh regardless of real-world elapsed time — decay only ever reaches a pilot deliberately left with no schedule at all.
- `ComputeEarnedSkill(hoursFlown, config)` exposes the growth curve alone, with no decay applied, so the UI can show "earned 68 from experience, currently 61" rather than a smaller number with no explanation — see `pilotSkillStatus` in `src/fsops-web/src/lib/pilotSkill.ts`, which turns `PilotSummary`'s server-computed fields into one of five status tones (never-decays, never-flown, decaying, grace-warning, normal) for the Pilots page.

**Surfacing it.** `AirlineEndpoints.GetReputationAsync` (`GET /api/v1/airline/reputation`) backs the Dashboard's reputation card: current score, a direction (`improving`/`declining`/`steady`/`new`) derived by comparing the average of the last 15 sectors' *targets* (not their outcomes) against the current score with a small threshold so a single borderline sector can't flip the label, on-time percentage and cancelled/skipped counts over that same window, a landing-quality bucket (`smooth`/`firm`/`hard`), and the exact demand multiplier this score currently applies (`DemandCalculator.ReputationDemandMultiplier`, the same function `AvailablePassengers` itself calls, so the card can never quote a number the demand engine doesn't actually use). `src/fsops-web/src/lib/reputation.ts` turns that summary into plain-language driver sentences and the demand-multiplier line, deliberately omitting a driver the backend couldn't measure rather than showing it as a false zero.

**Config.** Every figure above lives in `economy-config.json`'s `reputation`/`pilotSkill` blocks (`EconomyConfig.Reputation`/`PilotSkill`, `FSOps.Core.Economy`) and the reputation-to-demand mapping in `DemandConfig` (`ReputationBaselineScore` = 50, `ReputationSensitivity` = 0.25, `ReputationFloor` = 0.3 — solved so reputation 100 gives exactly 1.25× and reputation 0 gives exactly 0.75× the baseline demand) — shared across playstyles, not one of the `casual`/`trueLife` override blocks.

## The weekly schedule builder: aircraft-per-duty-day

The first build of the schedule builder organised everything by pilot, and let each leg pick its own aircraft independently. That let two legs in the same pilot's day reference two different airframes and pass every check cleanly, because `PilotScheduleValidator`'s continuity/overlap checks group by `FleetAircraftId` - two legs on two different aircraft in one day were simply never compared to each other. The redesign closes this by construction rather than by adding another check to catch the symptom: `PilotScheduleValidator.ValidateDutyDayAircraftConsistency` runs first, before geographic continuity or anything else, and groups every proposed entry by `(PilotId, DayOfWeek)` - if more than one distinct `FleetAircraftId` appears in that group, the whole save is rejected with every offending registration named, before any of the geometry checks even run. One aircraft per pilot per duty day is now a structural invariant the API layer cannot construct around, not a convention callers are trusted to follow.

**What this changes for the caller.** `PilotEndpoints` exposes two option endpoints that mirror the builder's two-step flow: `POST /pilots/{id}/schedule/aircraft-options` lists every fleet aircraft with an `eligible`/`reason` pair (ineligible for being reserved-for-player or grounded, in that priority order, matching the same "hard blocker before a one-click fix" ordering `FlightEndpoints.OptionsAsync` uses - see [Aircraft reservation](#aircraft-reservation-the-two-way-gate-and-reservationreconciler)), and `POST /pilots/{id}/schedule/leg-options` lists which legs *that already-chosen* aircraft can fly next, from wherever it will actually be. A reserved aircraft never reaches `PilotScheduleValidator` at all - `leg-options` short-circuits every route to `illegal` with the reservation reason directly, rather than running full validation only to reject on the same fail-fast structural check `Validate()` uses for the `PUT /schedule` save.

**`leg-options` splits conflicts into "before" (disqualifying) and "after" (a warning on an otherwise-legal option)** - a real-use defect fix, 2026-08-12. `PilotScheduleValidator` doesn't know about "before" or "after"; it only ever answers "is this whole entry set consistent". `GetLegOptionsAsync` runs it *twice* per candidate against two different slices of the same data to get that distinction: once against only the entries chronologically earlier than the requested slot (`beforeBaseline` - reservation/range/runway/first-leg-anchor/predecessor-continuity/rest-against-the-prior-duty-day all still fire here, since none of them depend on anything later), and once against the FULL baseline including whatever's already drafted later in the week. A conflict that only shows up in the second run - never the first - means the candidate is fine on its own but leaves something already on the calendar unresolved (most often: tomorrow's first departure), which is reported as a `warnings` entry on a still-`legal`, still-selectable option rather than as a reason to refuse it. This is what makes it possible to build a duty day up one leg at a time again: before this split, testing a single leg against the WHOLE week (requireWeekClosure only ever exempted the wrap pair, never an interior one) meant neither half of a new round trip could ever look legal in isolation, since inserting just the first leg necessarily left the aircraft unable to reach whatever the aircraft was already committed to next - a real pilot flying an identical daily round trip five days a week could not add a further leg at all, no matter which route or direction, because every candidate broke a "does the rest of the week still close" invariant that has no business gating one leg at a time. `SaveScheduleAsync` (`PUT /schedule`) is completely unaffected - it still runs `Validate()` once, straight, with `requireWeekClosure: true`, so an unclosed week is refused exactly as before; a `warnings` entry from `leg-options` is a preview of what save-time will still enforce, never a relaxation of it.

**`GET /pilots/schedule/overview`** is the airline-wide, read-only counterpart: every fleet aircraft as a row, its legs across the week, resolved once from every pilot's saved schedule and grouped by aircraft (or, in the frontend's second tab, by pilot) rather than edited from here - editing stays exclusively in the per-pilot schedule view. This is what makes "is my fleet actually being used" answerable at a glance instead of requiring a pass over every pilot individually.

`PilotScheduleValidator.Validate` accepts a `requireWeekClosure` flag: `PUT /schedule` passes `true` because a saved week must genuinely repeat (Saturday's last leg on an aircraft must connect to Sunday's first leg on the same airframe, and a pilot's last duty day must get its rest before the first comes back around), while the options endpoints pass `false`, because a week under construction is legitimately unclosed - the closing pair is skipped, but every interior pair is still checked in full. `WeekMinutes` (10,080) is the shared cyclical unit both paths reason in.

## Aircraft reservation: the two-way gate, and ReservationReconciler

`FleetAircraft.ReservedForPlayer` used to be a soft preference - one aircraft flagged, everything else fair game to both the player and the virtual-pilot scheduler, with nothing arbitrating a genuine conflict between the two. It is now a hard, mutually exclusive gate, enforced independently on both sides so the conflict is structurally unrepresentable rather than merely discouraged:

- **`FlightEndpoints.StartAsync`/`OptionsAsync`** only ever offer the player a `ReservedForPlayer` aircraft - an unreserved one shows disabled with `"Not reserved to you - reserve it from the Fleet page to fly it."` and `StartAsync` refuses outright with the same reasoning if called directly.
- **`PilotScheduleValidator.Validate`** rejects any entry whose aircraft is `ReservedForPlayer`, structurally and fail-fast (before the aircraft-per-duty-day or geometry checks even run) - and, as above, the builder's own option endpoints never offer a reserved aircraft as a candidate in the first place, so a player normally never even sees the rejection.
- **`FleetEndpoints.SetReservationAsync`** (`PUT /fleet/{id}/reservation`) is the sole place the flag ever changes post-creation. Releasing (`Reserved: false`) always succeeds immediately - a reserved aircraft was never claimed by the scheduler, so there is nothing on the other side to conflict with, even if the aircraft is mid-flight right now. Reserving (`Reserved: true`) an aircraft that already has active `PilotScheduleEntry` rows is refused with **409 Conflict** by default, naming every offending leg (pilot, day, route); passing `ForceClearSchedule: true` soft-deletes those entries and reserves the aircraft in the same call, echoing back exactly what was cleared.
- **The single-aircraft case is explicit, not automatic.** `AirlineEndpoints.CreateAsync` reserves the founding aircraft directly. `FleetEndpoints.EnsureSoleAircraftIsReservedAsync` runs at the moment a second aircraft is about to be added (from both the lease and buy paths) and re-asserts that the existing sole aircraft is reserved if it somehow isn't - "the moment reservation actually starts to matter." Nothing else in the app ever re-forces the flag once a fleet exists; from there it is entirely the player's own choice.

**`ReservationReconciler`** (`FSOps.Server.Services`) is the one-time-per-boot repair for a contradiction the old soft-preference model could leave behind: a database where an aircraft is marked both `ReservedForPlayer` and has active scheduled legs, written before this rule existed. It runs on **every** startup (not once, like a migration - a permanent safety net, not just a one-off fix), in a scope created immediately after migrations apply and strictly before any hosted service starts, because `VirtualFlightResolverService` never itself consults `ReservedForPlayer` and would otherwise resolve a contradictory occurrence on its very first pass. The resolution rule is fixed: **the schedule always wins** - a contradictory aircraft has its reservation released (`ReservedForPlayer = false`); its schedule is never touched, since silently deleting legs the player built is not this code's decision to make. If releasing leaves an airline with zero reserved aircraft, the reconciler reserves a replacement that has no scheduled legs of its own (preferring one parked at the home base, then the oldest), rather than risk recreating the exact contradiction it just fixed; if every remaining aircraft has legs, the airline is deliberately left at zero and recorded in the result for later surfacing. A clean database produces an empty result and writes nothing. See [Troubleshooting](guides/troubleshooting.md#a-restored-save-had-an-aircrafts-reservation-released) for what this looks like to a player restoring an old save.

## Getting rid of an aircraft, and settling a loan: the quote-then-confirm pattern

Three actions - selling an owned aircraft, ending a leased one's lease early, and settling a loan (fully or by overpayment) - share a deliberate pattern across `FleetDisposalEndpoints` and `FinanceEndpoints.SettleLoanAsync`/`OverpayLoanAsync`: **a `GET .../…-quote` endpoint precedes each commit, and the commit recomputes the same figure from current state and compares it, bit-for-bit, against what the caller confirmed.** A sale value depends on airframe hours and condition, which a virtual pilot's background flight can move; a lease-termination charge depends on wall-clock time, which always keeps moving; a loan's outstanding balance can move if `EconomyClockService`'s billing tick posts a payment against it in the same window a confirmation dialog is open. All three actions are irreversible, so on any mismatch the commit endpoint returns `400 Bad Request` with the fresh figure rather than silently posting a different number than the one the player saw and clicked. All time reads go through `IClock`, not `DateTimeOffset.UtcNow` directly, so the drift itself is deterministically testable.

- **`AircraftDepreciationCalculator.ComputeSaleValue`** (`FSOps.Core.Finance`) derives a resale factor from `NewAircraftResaleFactor` (0.80) minus weighted terms for condition and average A/C-check cycle progress, minus a further penalty if the aircraft is currently grounded, clamped to a configured floor - applied to the type's playstyle-scaled purchase price. Buying then immediately selling is always a loss; flying first and selling costs more still.
- **`LeaseTerminationCalculator.ComputeSettlement`** charges a pro-rata amount for the fraction of the current 30-day billing period already used (from `EconomyState.LastProcessedUtc`, the same watermark `EconomyClockService` owns) plus a flat `FeeMonths`-of-rent early-termination fee, posted as two separate itemised ledger lines.
- **`LoanSettlementCalculator`** computes a full-payoff quote (`RemainingBalance` plus a `FeePercentOfRemainingBalance` early-settlement fee) and applies an overpayment (reduces `RemainingBalance` only - the monthly payment is deliberately left unchanged, so an overpayment always shortens the term rather than lowering the payment; this is the only one of the three actions with no separate fee, since the fee is scoped to closing a loan out completely).
- **Shared blocking rules** (`FleetDisposalEndpoints.EvaluateDisposalBlockersAsync`): wrong ownership kind for the action (sell needs `Owned`, end-lease needs `Leased`), never while `FleetAircraftStatus.InFlight`, and never while the aircraft is on a virtual pilot's standing schedule (named, rather than silently unassigned). Grounded-for-maintenance is deliberately **not** a blocker for either disposal action - a worse sale figure, not a refusal, is the correct handling; selling explicitly does **not** pay off any outstanding loan, which is against the airline's cash flow rather than secured on a specific airframe, and the quote states this plainly.

## Repositioning a stranded aircraft

An aircraft that flies a one-way leg into an outstation with nothing going back out is stuck: it has nothing useful to do and no way home short of hand-flying an empty sector. `FleetRepositionEndpoints` (`GET /fleet/{id}/reposition-options`, `POST /fleet/{id}/reposition`) moves it, for a flat fee, without flying it there. It reuses the quote-then-confirm pattern above verbatim - the confirmed fee travels back as `RepositionAircraftRequest.ExpectedCost` and the commit refuses on any mismatch - and all time reads go through `IClock`.

- **The move is instant, and changes nothing but `LocationIcao`.** No `Flight` row, no block time, no airframe hours, no fuel burn, no maintenance-cycle progression, no condition loss. A ferry sector would be more realistic, but the feature exists to unstick an aircraft and a fix that itself takes a sector to apply is barely a fix; charging airframe hours would also quietly push the airframe toward its next A-check, which is a second unstated cost on top of the fee. Asserted as a test rather than left as a comment (`FleetRepositionEndpointsTests.Repositioning_IsInstant_AndChangesNothingButLocation`).
- **Destinations come from the airline's own network**, never the world airport table: `AircraftRepositionEvaluator.DestinationsFor` unions the departure *and* arrival ends of every **active** route (an outstation you only ever fly into is still somewhere you can move an aircraft back from) and removes wherever the aircraft already is. A deactivated route stops offering its airports, since an aircraft parked at an airport only a retired route touched is exactly as stranded as one anywhere else.
- **Every refusal rule lives in `AircraftRepositionEvaluator`**, which is pure and clock-free; the endpoint only supplies database state and turns the verdict into wording. The checks are ordered deliberately, following the same rule the Fly screen's unflyable reasons follow - *the blocker the player can do least about first*, so the reason shown always ends in an action that actually works: `InFlight` → `GroundedForMaintenance` → `NotReservedForPlayer` → `NoRoutesAtAll` → `NowhereElseToGo` → `AlreadyThere` → `DestinationNotServed` → `InsufficientCash`. Leading with "reserve it first" on an aircraft that is *also* in maintenance would send the player to do something that will not help.
- **Repositioning is player-only.** Only a `ReservedForPlayer` aircraft may be moved: one released to virtual pilots is theirs to fly, and moving it out from under them is not the player's to do without reserving it back first. This reuses the existing reservation gate (see [Aircraft reservation](#aircraft-reservation-the-two-way-gate-and-reservationreconciler)) rather than inventing a second notion of ownership, and it is why there is deliberately **no** standing-schedule check here despite disposal having one - `FleetEndpoints.SetReservationAsync` refuses to reserve an aircraft carrying active scheduled legs, so "reserved for the player" and "on a virtual pilot's schedule" cannot both be true. A schedule check would be unreachable code pretending to be a rule.
- **The fee is config-driven** (`EconomyConfig.AircraftRepositioning.Cost`, default and shipped value 2,000), shared across playstyles and validated as strictly positive - a free reposition would make aircraft placement meaningless, and a forgotten config block resolves to exactly 0. It posts one `LedgerCategory.AircraftRepositioning` line, its own category rather than `Other` or `Handling`, so the Finances page can answer "what have I spent moving aircraft around" on its own; the Finances page files it as a **variable** operating cost, since it buys nothing the airline still owns afterwards.

## The Finances page and the away-summary

**`FinanceEndpoints`** backs the Finances page. Every figure it returns that isn't explicitly named an estimate is summed straight from posted `LedgerTransaction`/`Flight` rows at request time - never a cached or recomputed total - so nothing on this page can ever disagree with what actually moved the cash balance, the same rule the rest of the app follows. The one deliberate exception is `PilotsAsync`'s `estimatedTotalCost`/`estimatedNetContribution`: `revenue` and `operatingCost` per pilot are real ledger sums (joined by `LedgerTransaction.FlightId`), but the comparison figure adds the pilot's `MonthlySalary` prorated to the requested window - and a prorated salary is never itself a ledger posting, since the real monthly wage line posts in full on its own cycle wherever that falls. The field names carry the `estimated` prefix specifically so the distinction survives into the frontend. `RoutesAsync` has no equivalent prorated cost (a route carries no monthly wage of its own), so every figure it returns is exact. `LeasesAsync`'s `nextPaymentUtc` is `EconomyState.LastProcessedUtc + EconomyClockService.PeriodLength` - a real rolling date, not a calendar-month assumption.

**`MaintenanceEndpoints`** backs two further things beyond the automatic accrual `MaintenancePoster` already applies at flight completion:

- **Perform maintenance now** (`GET /fleet/{id}/maintenance-quote`, `POST /fleet/{id}/perform-maintenance`) - the same quote-then-confirm shape as disposal. `MaintenanceScheduler.ApplyForced` always resets the chosen check's hours-since-check to zero regardless of how much of the interval had actually accrued, so bringing a check forward genuinely forfeits whatever hours were left; the quote shows the cost, downtime, hours remaining until the check would fall due naturally, and every affected schedule entry (pilot, day, route) before commit. Blocked while `FleetAircraftStatus.InFlight` or already `InMaintenance` - maintenance still never interrupts a flight in progress, and this endpoint is no exception to that rule.
- **The away-summary** (`GET /away-summary`, `POST /away-summary/acknowledge`) - everything resolved on the wall clock since `EconomyState.AwaySummaryLastViewedUtc`: ledger lines grouped by category, completed virtual-pilot flights (sectors/revenue/cost/net), maintenance events, and any flight recorded `Skipped`/`Cancelled`/`Suspended` in the window, with its reason. `GET` is deliberately side-effect-free (beyond a one-time bootstrap for a brand-new airline, so nothing is reported before anything could have happened) so polling or reloading mid-session never loses anything; only `POST /acknowledge` moves the cursor. A window shorter than a 5-minute threshold reports nothing, so a normal reload never trips it. The frontend's `AwaySummaryDialog` mounts once, app-wide, in `AppShell` rather than on any single page, so it surfaces on whatever route the player lands on first.

## The live operations map

`OperationsEndpoints.GetLiveAsync` (`GET /api/v1/operations/live`) backs the Dashboard's live operations map. It returns two things: every aircraft currently "in the air" (player and virtual, mixed together), and the airline's route network for the map's background arcs.

The player's own aircraft, if a flight is in progress, comes straight from `FlightLifecycleService.GetActiveSnapshot` — real telemetry, not interpolated, exactly as the Fly screen's own live view uses.

A virtual pilot's aircraft is never stored as "airborne" anywhere — it's computed fresh on every request from the schedule template plus the wall clock, which is what keeps it automatically consistent with `VirtualFlightResolverService`'s own eventual resolution (this endpoint never decides whether a flight is economically real; it only visualises where a currently-in-progress occurrence *would* be if it resolves the way its template says). For each schedule entry, `WeeklyOccurrenceCalculator` finds the most recent occurrence's departure time; if the block time (from `BlockTimeEstimator`, using the route's distance and the aircraft type's cruise speed) hasn't yet elapsed, the aircraft is airborne. Its position is interpolated along `GreatCircle.SamplePath` from the fraction of *airborne* time elapsed — taxi-out and taxi-in are deliberately excluded from the position fraction, so the aircraft sits at its departure or arrival airport while taxiing rather than sliding partway down the great circle before it's actually left the ground — with heading derived from the bearing to the next sample point, and phase mapped onto the same phase names `FlightPhaseStateMachine` uses so the two marker kinds read consistently on the map.

## The Statistics page

`StatsEndpoints` (`GET /stats/performance`, `GET /stats/fleet`, `GET /stats/pilots`) backs the Stats page, and follows the same discipline `FinanceEndpoints` already applies to money: every figure is derived at request time straight from posted `Flight` rows, never a cached or pre-aggregated total. Two things are deliberately **not** recomputed here rather than derived a second time:

- **On-time performance** uses exactly the same rule `AirlineEndpoints.GetReputationAsync` uses for the reputation card — completed, `Flight.InUtc` set, not `SimRateElevated` — so the Stats page and the Dashboard's reputation card can never disagree about whether a given sector counted as on time.
- **Revenue vs. cost and per-route profit and loss** aren't computed by `StatsEndpoints` at all — the frontend's `useStatsOverview` hook calls `FinanceEndpoints`'s own `/finance/costs` and `/finance/routes` directly, the same endpoints the Finances page calls. There is exactly one place either figure is ever calculated; the Stats page just charts it.

`PerformanceAsync` buckets by the *local completion day* (materialised into memory first, since SQLite's EF provider can't translate `GROUP BY`/`ORDER BY` over `DateTimeOffset` — see the project's EF/SQLite traps). A day only appears in the response if at least one sector completed that day, so a quiet stretch renders as an absent point rather than a fabricated 0%; `loadFactorPercent` is `PaxFlown` against the flown aircraft's own seat capacity, averaged over the day's sectors, and is `null` (never `0`) for a day where no flown flight could be matched to a known aircraft type. `FleetAsync` sums `hoursFlownInPeriod` from completed flights' own OOOI times via `BlockTimeCalculator`, not the lifetime `FleetAircraft.AirframeHours` counter, so it actually reflects the requested window, and its `hoursToNextACheck`/`hoursToNextCCheck` mirror `FleetEndpoints.ListAsync`'s own computation exactly, so the Fleet page and this page can never disagree about how close an airframe is to its next check. `PilotsAsync` — the pilot logbook — sums hours the same way per pilot (including the player, via `Pilot.IsPlayer`) and reuses the same on-time rule as `PerformanceAsync`.

All three endpoints accept an optional `days` query parameter (`ResolvePeriodDays`, defaulting to 30, capped at 365); the frontend's `PeriodSelector` offers 7/30/90-day presets. `useStatsOverview` fires all five requests (`/stats/performance`, `/stats/fleet`, `/stats/pilots`, `/finance/costs`, `/finance/routes`) in one `Promise.all` per period change, so the whole page loads or errors together rather than five components independently racing. Each table section (`FleetUtilisationTable`, `PilotLogbookTable`, and the performance chart) carries its own `ExportCsvButton`, so a CSV export always matches exactly the rows currently on screen for that period. `PerformanceChart` and `FinanceOverviewCharts` (both Recharts-based) are lazy-loaded behind `React.lazy`, same reasoning as the map components below — a player who never opens Stats never downloads a charting library at all.

## SimBrief flight plan import

`IFlightPlanProvider` (`FSOps.Core.Planning`) abstracts "something that can supply block fuel, cruise altitude, block time and a filed route for the sector about to be flown, or decline to." `FlightEndpoints` composes an ordered list of providers — `SimBriefFlightPlanProvider` first, `BuiltInFlightPlanProvider` last — via `ResolveFlightPlanAsync`, which returns the first provider that succeeds. `BuiltInFlightPlanProvider` always succeeds (it's the same deterministic `RoutePreviewCalculator` estimate used everywhere else), so this pipeline always produces a usable plan even if SimBrief has nothing, is unreachable, or the player never set a Pilot ID.

`SimBriefFlightPlanProvider` fetches `https://www.simbrief.com/api/xml.fetcher.php?userid={PilotId}` — SimBrief's free, keyless dispatch API — identifying the player purely by the `SimBriefPilotId` they've set in Settings. Every failure mode is a normal return value, never a thrown exception: no Pilot ID configured, an unrecognised Pilot ID or one with no plan on file (SimBrief answers HTTP 400 for both, indistinguishable from the status code alone), a timeout, an unparseable response — and, the one case FSOps must never silently paper over, **an OFP filed for a different city pair than the route actually being flown**, which is refused with a message naming both pairs rather than substituting mismatched numbers. This class is deliberately **server-side only** — it must never be reachable from the SPA, since the whole point of routing the fetch through the server is the app's no-external-network-call-from-the-browser rule (a direct browser call to simbrief.com would be both a CORS problem and a privacy one). The Pilot ID appears in the outbound request URL (SimBrief's own API contract leaves no other way to identify the player), but is never logged and never included in any exception message or response this class returns.

**What actually changes when a SimBrief plan is used, and what doesn't.** `FlightEndpoints.StartAsync` resolves a plan via the same pipeline the preview endpoint (`GET /flights/plan-import`, used by the Fly screen's `FlightPlanImportBanner` before the player commits) uses, and applies its `BlockTimeMinutes`/`BlockFuelKg` to `Flight.PlannedBlockMinutes`/`FuelPlannedKg` — the figures the report card later compares actuals against. This is enrichment only: it never touches the route, the aircraft, or the schedule, and — critically — it never changes what fuel is actually **charged**. Fuel billing stays anchored to the existing reconciliation path (live telemetry when the sim is connected, the deterministic estimate's `ChargedFuelKg` otherwise — see [Persistent fuel and tankering](#persistent-fuel-and-tankering)), exactly as it would for a flight planned with no SimBrief involvement at all. SimBrief changes what the plan *says*, never what the sector actually costs.

On the frontend, `useFlightPlanImport` (`src/fsops-web/src/hooks`) debounces `GET /flights/plan-import` behind route/aircraft selection so it re-fires automatically on a change, and separately exposes a `refresh()` that re-issues the same request immediately, bypassing the debounce and sharing one `AbortController` ref with the auto-fetch so an older in-flight request can never overwrite a newer one's result. This backs the Fly screen's **Check for OFP** button (`FlightPlanImportBanner`) — added because the auto-fetch alone only re-checks on a selection change, so a plan filed in SimBrief after the player had already settled on a route/aircraft here would otherwise need a reselect to appear. `FlightPlanImportBanner` itself renders three tiers: no Pilot ID configured (a dedicated prompt linking to Settings, nothing fetched), a fetch in progress or its result, and — regardless of `fromSimBrief` — the `message` string `PlanImportAsync` already returns verbatim, so every provider failure mode (unreachable, no plan on file, a mismatched city pair) surfaces with no client-side re-interpretation of what the server decided.

## The VATSIM ATC layer

`VatsimNetworkClient` (`FSOps.Server.Services`) fetches and caches VATSIM's public status feed (`vatsim-data.json`, documented at vatsim.dev, no API key, regenerating roughly every 15 seconds). It's registered as a singleton so every request for `/operations/atc`, from every connected client, shares one cache and one in-flight fetch rather than each poll independently hitting VATSIM's infrastructure — a `RefreshInterval` of 20 seconds (a margin above the feed's own 15-second regeneration) governs both a successful and a **failed** fetch equally: a timeout or malformed response is cached exactly like a good response, as an "unavailable" snapshot stamped with the attempt time. That's what makes this a backoff rather than a retry storm — VATSIM being down never costs more than one failed request per refresh interval, no matter how many clients are polling. Nothing from this feed is persisted; it's presentation data only, and nothing about a controller being online ever reaches the economy, reputation or ledger.

`VatsimEndpoints.GetAtcAsync` (`GET /operations/atc`) is deliberately narrower than the full VATSIM integration originally envisaged — which also covered drawing other people's live traffic on the map, verifying that a tracked flight was genuinely flown online against the player's own CID, and pre-filling the Fly screen from a filed flight plan. What shipped is the first slice: it surfaces **controllers only, never other pilots' traffic**, and only controllers **relevant to the player** — covering an airport in the airline's own active-route network, not a global controller list. "Relevant" is resolved locally against FSOps' own `Airports` table by matching the callsign's leading ICAO-shaped segment (`EGLL_TWR` → `EGLL`); en-route/oceanic callsigns (`_CTR`, `_FSS`, TRACON callsigns like `NY_APP`) never map to a single airport and are left out entirely rather than shown unplaced or guessed at, since FSOps holds no FIR/sector boundary data that would let it say whether one of those genuinely covers the network. The frontend polls this endpoint (never VATSIM directly) every 45 seconds via `useVatsimAtc`, and — matching `useLiveOperations`'s own convention — only the very first load can surface an error state; once a good response has landed, a later poll failing keeps showing the last good data rather than flashing an error over a working map. `AtcControllerList`/`AtcCountBadge` render the same three states (loading, unavailable, empty) the map layer itself shows, so the list and the map never disagree.

### Sector geometry, and what the map refuses to draw

En-route control is drawn for the first time. CTR/FSS positions — which this endpoint always dropped entirely, because a callsign like `LON_CTR` resolves to no single airport — now render as their real published FIR/UIR boundary, from **VAT-Spy data bundled with the app** (`data/vatspy/`, CC BY-SA 4.0, shipped verbatim and parsed at runtime). Bundled rather than fetched because FSOps has to work with no internet connection at all; **verbatim** rather than pre-processed because a derived file would be a database adaptation and would itself have to carry the share-alike licence, dragging the obligation into FSOps' own source.

Two representations, kept visually distinguishable because they are not the same kind of claim:

- **Sector** — a real polygon, solid outline, and deliberately **no marker**. A sector is an area; a pin would put a controller somewhere nobody is.
- **Terminal** — airport-local TWR/GND/DEL and airport-named APP keep a visual-range circle, drawn **dashed and fainter** so a sketch never carries a fact's visual weight. That circle is how far the controller's client is configured to see, not the shape of anything they control.
- **Everything else stays absent.** Non-airport TRACONs (`NY_APP`) resolve to neither and are still dropped. There is no published boundary data for them, and an invented shape would be worse than silence — a wrong boundary on a map reads as authoritative.

The legend states all of this on screen rather than in documentation, including that coverage is lateral only: the data carries no altitude limits, so a polygon says nothing about which levels are being worked, and top-down coverage is never inferred.

**`ADR_E` — why prefix matching is longest-first.** The obvious implementation takes the callsign segment before the first underscore. Upstream prefixes contain underscores themselves: `ADR_E` is Adria Radar *East*. First-segment matching would have resolved `ADR_E_CTR` to the whole of Adria — a boundary several times too large, drawn with complete confidence, and **no test would have caught it**, because the code would have matched its own specification exactly. It was found only by reading the real bytes rather than trusting the format description. The reasoning is recorded beside the matcher and in a test named for it, because "simplify this to split on the first underscore" is the natural next edit.

**One fetch, four consumers.** `VatsimNetworkClient` now also parses the feed's pilot positions, which were previously discarded entirely. Everything shares that single cached fetch rather than polling the feed separately: the ATC layer, `GET /vatsim/traffic` (other pilots near the network), and `VatsimFlightCorroborationService`, which matches a tracked flight's configured CID against FSOps' own telemetry — **position and timing, never presence alone** — and records `Flight.VatsimOnline` at finalize.

**`GET /vatsim/history` is deliberately NOT a second call to VATSIM.** It reads FSOps' own `Flight.VatsimOnline` records. VATSIM's actual history API is key-gated — reading any CID's past sessions is a real privacy surface, so it is not handed out keylessly — and an unauthenticated best-effort call would be a permanent-failure outbound request achieving nothing. Worse, it would fail *silently*: broken and "you have flown nothing online" look identical. The UI says plainly that this is FSOps' own record so an absent flight never reads as VATSIM having lost it.

A corroborated flight earns a modest, config-driven revenue and reputation bonus, computed server-side from the recorded corroboration only, and only on a sector that was payable anyway — a slewed or jumped flight earns no bonus exactly as it earns no fare.

**The list follows the viewport.** The endpoint can return everything FSOps can place worldwide, and the client filters to what is currently visible from data it already holds — panning never refetches, and the 15-second feed cadence is untouched. Network relevance became **emphasis rather than a filter** (filled icon, sorted first): hiding a controller whose sector is drawn on screen would make the map and the list disagree, and the user is looking at both at once. A consumer with no viewport keeps the original network scope.

## How the SPA is chunked

Every page is loaded through `React.lazy` behind a single `Suspense` boundary in `App.tsx`, and the three components that mount MapLibre (`LiveOpsMap`, `RouteMap`, `LiveFlightMap`) are lazily loaded again inside their own pages. `vite.config.ts`'s `manualChunks` deliberately groups **only** `react`/`react-dom`/`scheduler` into a `vendor-react` chunk; a broader catch-all was tried and rejected, because it swept MapLibre into a statically-imported bucket and forced it to load eagerly again.

MapLibre is by far the heaviest dependency in the app — roughly 800 kB raw on its own, over half the old single bundle's gzipped weight — and most of the app never needs a map. Splitting it out took the eager first paint from about 425 kB gzipped to about 134 kB, and Fleet, Pilots, Finances, Statistics and Settings now never fetch MapLibre at all.

One honest caveat: the Dashboard's `LiveOpsMap` is gated on the live-operations request completing rather than on any user action, so MapLibre *is* fetched there shortly after the shell renders. It no longer blocks first paint, but the dashboard is not a map-free route.

Every lazy boundary needs a designed fallback, not a blank area — `PageSkeleton` for routes, and map-sized `Skeleton` blocks for the map components so nothing shifts when they arrive.

## World data: refreshing without destroying history

`WorldDataImporter` originally returned early whenever *any* airport row existed — the guard asked "are there airports?", not "is this data current". So the ~44k airports imported on a user's first launch were frozen forever, and shipping fresher CSVs in a new release changed nothing for an existing install.

**The stamp is a content hash, not a version number.** A SHA-256 of both bundled `.gz` files plus an `ImporterVersion` constant, written to `worlddata.stamp.json` in the data directory. A hand-maintained number needs someone to remember to bump it, and the once it is forgotten every existing install silently keeps stale data with nothing reporting a problem; hashing makes "the data changed" and "the stamp changed" the same fact. `ImporterVersion` is part of the identity so that changing the CSV-to-entity mapping re-imports unchanged files.

It is a **file beside the database rather than a table**, so no migration is involved: it describes a bundled asset, not anything the player owns. The cost is that the file and the tables can disagree, so the two possible disagreements are deliberately asymmetric — the stamp is written **only after the transaction commits**, and the presence of airport rows is checked **independently** of it. A lost stamp costs one harmless extra refresh; a deleted database re-seeds regardless of what the stamp claims.

**A refresh upserts and never deletes, and the reason is stronger than "it would orphan history".** `Route.DepartureIcao`/`ArrivalIcao`, `FleetAircraft.LocationIcao` and `Airline.HomeAirportIcao` reference airports by **plain string with no foreign key** — only `Runways` has an FK to `Airports`. So deleting a vanished airport would not fail loudly or cascade; it would silently leave routes pointing at nothing. OurAirports also removes entries for editorial reasons at least as often as for real closures, and the app cannot tell the two apart. A vanished airport is therefore kept untouched, along with its runways. Runways *are* replaced per airport — nothing the player owns references a runway row — but only for airports actually present in the new bundle.

Two defects found while building it, both of the "looks like success" kind: the reported runway count was rows *processed* rather than rows in the table, so a refresh that correctly **kept** data under-reported by exactly the amount it had preserved — which reads on screen as data loss; and a runway file that decompressed to nothing committed silently, because a truncated gzip does not reliably throw. That second one would have stamped a damaged bundle as applied, so the app would never retry and every runway-length check would read as unknown forever. It is now refused and rolled back.

## Background work in FlightLifecycleService is gated on the sample, never the wall clock

Every background task the flight tracker starts — broadcasts, finalisation — is gated behind `ProcessSample`'s cadence, and that cadence is keyed off **the sample's own timestamp**, not `DateTimeOffset.Now`. This is not incidental. It is what allows a test that drives samples synchronously to control exactly when background work fires and to know it has finished before the test disposes its database connection.

Break that discipline and the failure does not look like a design mistake — it looks like flaky tests. A fire-and-forget database lookup started on real wall-clock timing at the moment tracking begins can still be mid-query when a test's `using var ctx` disposes the connection underneath it, producing `SQLite Error 5: unable to delete/modify user-function due to active statements` in whichever unrelated test happens to run alongside it. That is precisely how it was found: an eager fire-and-forget added for VATSIM corroboration made two unrelated ledger and fuel test classes fail intermittently, and the obvious reading was parallel-execution noise.

**So: new background work goes inside the sample-gated cycle.** If something genuinely must run once rather than per tick, resolve it on the first gated tick rather than eagerly at start. The rule costs nothing and the alternative is intermittent failures that get blamed on the test runner.

## Range: one question, asked about the airline

`RouteRangeAssessor` (`FSOps.Core.Planning`) exists because the original check asked the wrong question. `RoutePreviewCalculator` compared the distance against **one** aircraft type and refused the route naming it — *"beyond the Airbus A320's practical operating range"* — as though that airframe were the airline's entire capability. It was wrong in two directions at once: the player might own or be able to reserve something with the range, and the aircraft named might be one they could never fly on that route anyway.

The assessor answers about the **whole fleet**, and returns which of four situations applies:

- **`ReservedCanFly`** — an aircraft reserved to the player can do it. Say nothing.
- **`RequiresReservation`** — nothing reserved can, but the fleet has something that can. **This never blocks.** It names the aircraft and points at reserving it. This is the case the original code refused outright, and the reason the whole change exists.
- **`BeyondFleet`** — nothing owned can fly it. The only genuine block.
- **`NotAssessed`** — no fleet yet; a non-blocking note stating what the figures assumed.

Aircraft selection within a case is deterministically ordered, so the same fleet always names the same aircraft rather than whichever row the database happened to return first.

**The rule is enforced at every point an aircraft is actually chosen, not just where a route is created.** Route creation is guidance; scheduling and flying are enforcement:

- **Scheduling** (`PilotScheduleValidator`) treats range as one more structural refusal alongside wrong-airport, reserved, grounded and no-rest-room — one plain reason per route/aircraft pair, deduplicated so a five-day repeat is one message rather than five. Deliberately the existing mechanism; a second one would have meant two places to keep in step.
- **The Fly screen** (`FlightEndpoints`) lists an unreachable airframe but marks it unflyable, and reports the range reason **ahead of** "not reserved to you" — because reserving it would not help. `StartAsync` enforces it server-side too: filtering the options list alone is presentation, and a stale client or a direct call would otherwise still start an unreachable sector.

The 0.85 operational-range factor is unchanged and now defined once rather than repeated. A test asserts that no possessive aircraft-type name can come back from route validation, so the old framing cannot quietly return.

## The desktop shell and how FSOps is published

`src/FSOps.Desktop` (`net8.0-windows`, x64, WinForms hosting `Microsoft.Web.WebView2`) is what a user actually launches. It exists so FSOps is an application with its own window, taskbar entry and icon rather than a browser tab that can be closed independently of the server still running behind it.

**The server runs as a child process, not in-process.** Two reasons, and the second is the stronger one. `FSOps.Server`'s composition root is top-level statements with no callable entry point, so in-process hosting would have meant restructuring `Program.cs`. More importantly, the server must stay independently launchable — headless runs and every integration test start it directly — and one startup path that everything shares is worth more than two that can drift apart. The orphan risk this creates is answered by `ChildProcessJob`, a Windows Job Object with `JOB_OBJECT_LIMIT_KILL_ON_JOB_CLOSE`: kernel-enforced, so the server dies even when the shell is killed abruptly from Task Manager rather than closed tidily.

**The asymmetry in that rule is deliberate and must not be "tidied up".** If the chosen port is already answering, the shell **attaches** to that server instead of starting a second one — there is one SQLite database per user — and on close it deliberately leaves that server running, because it did not start it. The job object never adopts a process the shell did not launch. This mirrors a hard rule that has been broken repeatedly in this project's history: only ever stop the process you started. Launching the desktop app while a browser copy is already running does the right thing, and killing the desktop app does not take the user's other session down with it.

**A missing WebView2 runtime is never a dead end.** The display mode is decided *before* any window is constructed, so nobody sees a dead frame. Without WebView2 the SPA opens in the default browser and the app carries on working; the window still appears, small, owning the server's lifetime and giving a taskbar entry, saying in one line what happened and offering the runtime as an improvement rather than a prerequisite. It also falls back if WebView2 is present but fails to initialise later. `FSOPS_USE_BROWSER=1` forces this mode deliberately.

**The bug this nearly shipped with, recorded because it is a whole class of bug.** The WebView2 package delivers its native loader at `runtimes\win-x64\native\WebView2Loader.dll` as **plain content**, so it never appears in the app's `.deps.json` and the .NET runtime never adds that folder to the native DLL search path. A framework-dependent build survives by finding the loader in the NuGet cache; a self-contained publish has no such fallback and throws `0x8007007E` before a window exists. It was invisible under `dotnet run` and fatal for the installed app — exactly the failure mode the project's "test it the way a user launches it" rule exists for, and the third time that rule has paid for itself. MSBuild targets now copy the loader beside the executable in both build and publish output, and `scripts/publish.ps1` asserts its presence rather than trusting it.

**Publishing** is `scripts/publish.ps1`: builds the SPA, publishes both projects self-contained for `win-x64`, and asserts the resulting layout. The output is a **flat single folder** (~536 files, ~188 MB) meant to be copied verbatim. `FSOps.Server.exe` is located relative to `FSOps.Desktop.exe`, so the two must never be separated, and `WebView2Loader.dll` must remain at the root.

**The fixed-version WebView2 distribution was considered and rejected.** It roughly doubles the installer for a case already covered twice over, and it is a browser engine receiving no automatic security updates — FSOps would own patching Chromium in its own release cycle forever, and a stale engine is a worse outcome than no engine.

## The update checker

`UpdateChecker` (policy), `GitHubReleaseClient` (transport), `UpdateStateFile` (persistence) and `UpdateEndpoints` (`GET /update/status`, `POST /update/check`, `PUT /update/preferences`, `POST /update/dismiss`, `POST /update/download`, `POST /update/reveal`).

**How far it goes, and why it stops there.** Check → notify → *the user clicks* → the server downloads → SHA-256 verify → open the containing folder. **It never runs the installer.** FSOps ships unsigned, so the published checksum is the only thing separating "the installer the author built" from "whatever arrived over the network". An app that silently executed a downloaded binary would be constructing precisely the attack path the checksum exists to close, using trust the user extended to FSOps rather than to what it fetched. The reveal endpoint hands ShellExecute only the **directory**, never the installer's own path, so no code path in FSOps can launch it even by mistake.

**Download ordering is load-bearing.** The checksum is fetched *first* — no checksum means nothing is downloaded at all — then the asset streams to a `.part` name, is hashed **from disk** after the write completes, and only then moved into place. A failed or interrupted download can therefore never be mistaken for a verified one. Verification is re-checked at reveal time too, so a file swapped after verification is refused and deleted.

**Nothing about updates is on the startup path.** There is deliberately no hosted service: the check is lazy and its result cached on disk at `%LOCALAPPDATA%\FSOps\update-state.json`. That file, rather than `UserSettings`, holds the preference precisely because a kill switch that cannot be read unless the database is up is not a kill switch. Switched off means no outbound request is attempted at all — verified live, not merely intended.

**Every expected failure is a failed result, never an exception**: no network, timeout, malformed payload, rate-limiting, missing asset, missing checksum, draft, pre-release, or an older or equal version. HTTPS and a GitHub host allowlist are enforced before a byte is requested, and size caps against bytes actually written rather than a declared length.

**On the "no external network" rule.** That constraint is a **frontend** one — the SPA makes no external calls, everything going through FSOps' own API. Server-side this is the **second** outbound call, after VATSIM, not the first.

Expected release shape: a non-draft, non-prerelease release tagged `v<major>.<minor>.<patch>`, carrying `FSOps-Setup-<version>.exe` and a sidecar named exactly `<installer name>.sha256`. The installer derives its version from `Directory.Build.props`, the same source `AppVersion` reads back, so the artefact name and the running version cannot drift — if they did, everyone already on the new version would be offered it forever.

## Testing

**Backend:** `tests/FSOps.Core.Tests` and `tests/FSOps.Server.Tests` are xUnit, run with `dotnet test` from the repository root — see [Solution layout](#solution-layout) for what each covers.

**Node version:** the frontend tests need **Node 22 or newer** — jsdom's bundled `undici` calls `webidl.util.markAsUncloneable`, which does not exist on Node 20, and the failure is not a failing test but the whole Vitest run dying at import with zero tests executed. Worth knowing because the symptom looks nothing like the cause.

**Frontend:** `src/fsops-web` uses [Vitest](https://vitest.dev/) (native to Vite, so it shares the project's existing config rather than needing a second build pipeline), run from `src/fsops-web` with `npm test` (watch mode) or `npm run test:run` (single pass, what CI-style checks should use). Configuration lives directly in `vite.config.ts`'s `test` block: `jsdom` environment, `globals: false` (tests import `describe`/`it`/`expect` explicitly from `vitest` rather than relying on ambient globals), a setup file at `src/test/setup.ts`, and `restoreMocks: true` so a mock in one test can never leak into the next.

Test files sit beside the code they cover (`*.test.ts`/`*.test.tsx`, matched by `include: ['src/**/*.test.{ts,tsx}']`), not in a parallel directory — `src/lib/format.test.ts` next to `src/lib/format.ts`, `src/components/schedule/scheduleMath.test.ts` next to `scheduleMath.ts`, and so on. As of this writing that covers: pure formatting and date logic (`lib/format.test.ts`), the reputation and pilot-skill display helpers described above (`lib/reputation.test.ts`, `lib/pilotSkill.test.ts`), the schedule grid's layout maths and draft-entry state (`components/schedule/scheduleMath.test.ts`, `draftEntry.test.ts`), and the request-body/reset behaviour of four confirm-then-act dialogs (`components/finance/LoanRepaymentDialog.test.tsx`, `components/fleet/SellAircraftDialog.test.tsx`, `EndLeaseDialog.test.tsx`, `RenameAircraftDialog.test.tsx`).

The original scope was deliberately narrow, not an oversight: the suite exists because two shipped bugs slipped past `tsc` and a compiling build — a "pay off loan in full" button that posted no request body to an endpoint requiring the confirmed figure, and a confirmation input that reset mid-keystroke because a `useEffect` depended on object identity rather than a primitive. Both are exactly the class of bug `tsc` cannot see (it type-checks each side of the API boundary independently, never that a request body actually matches what an endpoint expects) and a runner catches immediately.

**A rendering layer was added later, deliberately and on the original terms.** Component tests now cover finance sections, maintenance dialogs, fleet utilisation, and shared primitives. What was rejected then is still rejected now: **no DOM snapshots, no pixel comparisons, no asserting on class names or Tailwind utilities.** Tests assert on behaviour and content — what a component renders for a given state, and whether a disabled control is disabled for the reason it claims. The recurring theme is the distinction between **"could not load"** and **"you have none"**: every section is pinned to never show the second when the first is true, because telling a player with debt that they have no loans is the most damaging confusion that page can produce.

`@testing-library/react` was considered and **not** added. `src/test/domHarness.ts` grew role and accessible-name queries instead, keeping the suite dependency-free and offline-safe — and it carries an explicit marker saying that needing full accessible-name computation, visibility filtering, or async `findBy*` is the signal to add the real dependency rather than keep extending a shadow copy of it.

The tests were checked for teeth rather than assumed to have them: three behaviours were deliberately broken — an A/C-check comparison flipped, a disabled pagination button re-enabled, a minus sign dropped from a loss-making sector — and exactly the expected tests failed, with none firing spuriously and none staying silent.

### Repository hygiene

`scripts/repo-hygiene.ps1` runs first in CI and blocks. It asks the question the old by-hand scan never did: **not "is this content dangerous" but "does this file have any business existing".** That gap is not hypothetical — an empty file named `check` sat in the root of this public repository for two days, because every rule was looking for secrets and none was looking for junk.

It covers three things: files that should not exist (stray root files, build output, editor and OS droppings, empty tracked files), content that must never ship (private keys, access keys, connection strings carrying passwords, database files, and any reference to local-only tooling), and anything excluded via `.git/info/exclude` that has been committed anyway.

Two decisions matter more than coverage. **Every rule explains itself when it fires** — what was found, why it matters, what to do — because a check that only says "no" is a check people learn to route around. And **an exception is made by name, in public, with a reason**, via the `$Allowed` table, rather than by loosening a rule for everything. It also **never deletes anything**: a script that removes files from a repository unattended is a worse problem than the one it solves.

It was proved to fire rather than assumed to: planting an empty `check` in the root reproduces the original case and trips three rules at once.

## Layering diagram

```
┌─────────────────────────────────────────────────────────┐
│                    src/fsops-web (SPA)                   │
│         React + TypeScript, Vite, Tailwind, shadcn/ui    │
└───────────────────────────┬───────────────────────────────┘
                REST /api/v1 │ SignalR /hubs/live
┌───────────────────────────▼───────────────────────────────┐
│                     FSOps.Server                          │
│   API endpoints · SignalR hubs · DI composition · statics │
└───────┬───────────────────────────────────────────┬───────┘
        │                                           │
┌───────▼───────────────┐                 ┌─────────▼─────────┐
│      FSOps.Core        │                 │     FSOps.Sim      │
│ Domain model, economy   │                 │  SimConnect adapter │
│ (deterministic, tested) │                 │  (swappable source) │
└───────┬────────────────┘                 └─────────┬─────────┘
        │                                             │
┌───────▼────────────────┐                            │
│      FSOps.Data         │                            ▼
│  EF Core ──► SQLite      │                     MSFS 2024 (SimConnect)
└──────────────────────────┘
```

`FSOps.Server` is the only project that depends on ASP.NET Core; `FSOps.Core` stays free of framework, persistence, and simulator dependencies so it can be tested and reasoned about on its own.
